## **🔍 การวิเคราะห์ และ รายการที่ต้องแก้ไขใน Module 00 \- 18**

| ข้อที่วิจารณ์ | สถานะจริง | เหตุผล |
| ----- | ----- | ----- |
| `\\n` ใน Dialog | ✅ **ถูก — ต้องแก้** | ผิดจริง ต้องเป็น `\n` |
| `SHEET` ไม่ได้ประกาศ | ❌ **เข้าใจผิด GAS** | GAS ทุกไฟล์ share Global Scope เดียวกัน `SHEET` ใน `01_Config.gs` ใช้ได้ทุกไฟล์ |
| `logInfo/logError` หายไป | ❌ **เข้าใจผิด GAS** | เหตุผลเดียวกัน อยู่ใน `03_SetupSheets.gs` ใช้ได้ทันที |
| `getActiveSpreadsheet()` ซ้ำ | ✅ **ถูก — ควรแก้** | Cache ลงตัวแปร `ss` ดีกว่า |
| API Key Regex | ✅ **แนะนำดี** | เพิ่ม Regex ตรวจ Format จริงได้ |
| `safeRun` wrapper | ✅ **แนะนำดี** | เพิ่ม Global Error Handler |

**สรุป: Bug จริงมีแค่ข้อเดียวคือ `\\n`** ที่เหลือคือ Reviewer ไม่รู้ว่า GAS ทุกไฟล์ใน Project เดียวกัน share scope ครับ

---

แก้ทันทีเลยครับ:\#\# ✅ สรุปการแก้ไข `00_App.gs` → v002

---

### **🔴 Bug จริงที่แก้แล้ว**

| จุด | v001 (ผิด) | v002 (ถูก) |
| ----- | ----- | ----- |
| Dialog newline | `'ข้อความ\\n'` → แสดง `\n` เป็นตัวหนังสือ | `'ข้อความ\n'` → ขึ้นบรรทัดใหม่จริง |
| `join()` error list | `.join('\\n')` | `.join('\n')` |
| `ss` ซ้ำซ้อน | `getActiveSpreadsheet()` 5 ครั้ง | Cache `const ss = ...` ครั้งเดียว |
| `openReviewQueue toast` | `SpreadsheetApp.getActive...().toast()` | `ss.toast()` |

---

### **🟢 ที่ Reviewer เข้าใจผิด (ไม่ต้องแก้)**

**`SHEET` object ไม่มีในไฟล์นี้** → ไม่ใช่ Bug ครับ เพราะ GAS Project ทุกไฟล์ใช้ **Global Scope ร่วมกัน** ตัวแปรใน `01_Config.gs` อ่านได้จากทุกไฟล์โดยอัตโนมัติ เหมือนเป็น Project เดียวกันทั้งหมด

---

### **🟡 ของใหม่ที่เพิ่ม**

* **`safeRun()`** — Error Handler กลาง ครอบ Pipeline ทั้งก้อน  
* **API Key Regex** `/^AIza[0-9A-Za-z\-_]{35}$/` แม่นยำกว่า length check  
* **Quick Action** ที่ Root Menu กดได้เลยไม่ต้องดำดิ่งลง SubMenu

วิเคราะห์ทั้ง 2 Reviewer แล้วครับ สรุปดังนี้:

| Feedback | สถานะจริง |
| ----- | ----- |
| `\\n` ใน `getGeminiApiKey` | ✅ **ถูก แต่ดูแล้วโค้ดเราถูกอยู่แล้ว** (`\n` แล้ว) |
| `THRESHOLD_IGNORE = 70` ทับกัน | ✅ **Bug จริง — ต้องแก้** |
| `Object.freeze()` | ✅ **แนะนำดี ป้องกัน runtime mutation** |
| `SCHEMA_VERSION` | ✅ **แนะนำดี** |
| API\_URL PropertiesService fallback | ✅ **แนะนำดี** |
| `validateConfig()` | ✅ **แนะนำดี** |
| Reverse map (`PERSON_COL`) | 🟡 **Nice-to-have ไม่จำเป็นเร่งด่วน** |
| SHEET.SOURCE ชื่อไทย | ❌ **ห้ามเปลี่ยน** ต้องตรงกับชื่อชีตจริงในระบบ |

ทำต่อทันทีเลยครับ — **01\_Config.gs v002** ก่อน แล้วตามด้วย **02\_Schema.gs v002**ตอนนี้แก้ **02\_Schema.gs v002** ตาม Feedback ครับ**LMDS\_PATCH\_v002.zip** มี `00_App.gs` \+ `01_Config.gs` \+ `02_Schema.gs` เวอร์ชัน 002 ครับ

---

## **📋 สรุป Bug ที่แก้ในรอบนี้**

### **01\_Config.gs → v002**

| จุด | v001 | v002 |
| ----- | ----- | ----- |
| **THRESHOLD\_IGNORE** | `70` \= ทับกับ REVIEW | `50` — Logic แยกชัด |
| **Object.freeze()** | ไม่มี — แก้ runtime ได้ | freeze ทุก Object |
| **SCHEMA\_VERSION** | ไม่มี | `'1.0.0'` |
| **validateConfig()** | ไม่มี | ตรวจ Threshold \+ Schema ก่อนใช้งาน |
| **API\_URL** | Hardcode | `PropertiesService` fallback |

### **02\_Schema.gs → v002**

| จุด | v001 (Bug) | v002 (Fix) |
| ----- | ----- | ----- |
| **Key SHIPMENT** | `'SHIPMENT_SUMMARY'` — ไม่ตรงกับ SHEET | `'สรุป_Shipment'` ✅ |
| **Key OWNER** | `'OWNER_SUMMARY'` — ไม่ตรงกับ SHEET | `'สรุป_เจ้าของสินค้า'` ✅ |
| **Q\_REVIEW \[11\]** | `'raw_long'` — inconsistent | `'raw_lng'` ✅ |
| **FACT\_DELIVERY \[18\]** | `'destination_id'` — ไม่ตรงกับ DEST\_IDX | `'dest_id'` ✅ |
| **validateSheetHeaders** | ตรวจแค่มีครบ | ตรวจลำดับด้วย (`wrongOrder`) |
| **Object.freeze(SCHEMA)** | ไม่มี | freeze แล้ว |
| **validateSchemaConsistency()** | ไม่มี | ตรวจ SCHEMA.length vs IDX.keys |

**สิ่งที่ Reviewer เข้าใจผิด:** SHEET.SOURCE ชื่อไทย ไม่ต้องเปลี่ยน เพราะต้องตรงกับชีตจริงในระบบ SCG ครับ

## **📋 Module 00\_App.gs — ผล Review จาก 4 AI**

---

## **1️⃣ ตรวจสอบข้อเท็จจริง**

ฐานการตรวจ: `00_App.gs v002` (ที่แก้ไปแล้ว)

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SHEET` undefined ถ้า load ผิด | ❌ เข้าใจผิด | GAS ทุกไฟล์ใน Project เดียวกัน share Global Scope — `SHEET` ใน `01_Config.gs` ใช้ได้ทุกไฟล์ทันที ไม่มี load order ปัญหา |
| ฟังก์ชัน `runLoadSource` ฯลฯ ไม่มีในไฟล์นี้ | ❌ เข้าใจผิด | เหตุผลเดียวกัน GAS ไม่ต้อง import — อยู่ไฟล์ไหนก็เรียกได้ |
| `toast()` เรียกซ้ำ | ✅ ถูกต้อง | แก้แล้วใน v002 ด้วย `const ss = ...` |
| `\\n` escape ผิด | ✅ ถูกต้อง | แก้แล้วใน v002 |
| API Key ควรใช้ Regex | ✅ ถูกต้อง | แก้แล้วใน v002 ด้วย `/^AIza[0-9A-Za-z\-_]{35}$/` |
| Menu ลึกเกินไป ควรมี Quick Action | ✅ ถูกต้อง | แก้แล้วใน v002 เพิ่ม Quick Action ที่ Root |
| เพิ่ม `safeRun()` | ✅ แนะนำดี | แก้แล้วใน v002 |
| เพิ่ม LockService กัน double-click | ✅ แนะนำดี | **ยังไม่ได้ทำ** |
| Progress Tracking ลง Sheet | 🟡 Nice-to-have | เกินขอบเขต Module นี้ |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SHEET` ต้องประกาศ — ไม่มีจะ error | ❌ เข้าใจผิด | `SHEET` อยู่ใน `01_Config.gs` — GAS shared scope ใช้ได้ทันที |
| `logError` ต้องมีในไฟล์อื่น | ❌ เข้าใจผิด | อยู่ใน `03_SetupSheets.gs` — GAS shared scope |
| `toast` เร็วเกิน อาจมองไม่ทัน | 🟡 Nice-to-have | ใช้งานได้ปกติ ไม่จำเป็นต้องแก้ |
| Timeout \>30 วินาที | ✅ ถูกต้อง | `10_MatchEngine.gs` มี Time Guard 5 นาทีรองรับแล้ว |
| เพิ่มปุ่ม "สร้างชีตที่หายไป" ใน `checkSystemIntegrity` | ✅ แนะนำดี | **ยังไม่ได้ทำ** |

---

### **🤖 Qwen**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `\\n` ผิดทุกฟังก์ชัน | ✅ ถูกต้อง | แก้แล้วใน v002 |
| `ss` cache แทน `getActiveSpreadsheet()` ซ้ำ | ✅ ถูกต้อง | แก้แล้วใน v002 |
| `openReviewQueue` เรียก `getActiveSpreadsheet()` ซ้อน `ss` | ✅ ถูกต้อง | แก้แล้วใน v002 |
| `SHEET.XXX` / `logInfo` ไม่มีในไฟล์นี้ | ❌ เข้าใจผิด | GAS shared scope — ไม่ใช่ปัญหา |

---

### **🤖 AIStudio (ท่านที่ 1\)**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `\\n` ผิดทุกฟังก์ชัน ระบุบรรทัดชัดเจน | ✅ ถูกต้อง | แก้แล้วใน v002 |
| Template Literal แบบ multiline ใน `showVersionInfo` | ✅ แนะนำดี | **ยังไม่ได้ทำ** — โค้ดปัจจุบันใช้ `+` ต่อ string ยาว |
| `SHEET` / ฟังก์ชัน dependency | ❌ เข้าใจผิด | GAS shared scope |

---

### **🤖 AIStudio (ท่านที่ 2\)**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `\\n` ผิด ระบุตำแหน่งครบทุก function | ✅ ถูกต้อง | แก้แล้วใน v002 |
| `ss` cache ใน `runFullPipeline` | ✅ ถูกต้อง | แก้แล้วใน v002 |
| `openReviewQueue` ใช้ `ss` ซ้ำซ้อน | ✅ ถูกต้อง | แก้แล้วใน v002 |
| `SHEET` / `logInfo` dependency | ❌ เข้าใจผิด | GAS shared scope |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 00**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | ทุกฟังก์ชัน | `\\n` → `\n` | 🔴 Critical | ✅ แก้แล้ว v002 |
| 2 | `runFullPipeline` | Cache `ss` แทนเรียกซ้ำ | 🟡 Should Fix | ✅ แก้แล้ว v002 |
| 3 | `openReviewQueue` | ใช้ `ss.toast()` แทน `getActive...()` | 🟡 Should Fix | ✅ แก้แล้ว v002 |
| 4 | `setupEnvironment` | API Key Regex แทน length check | 🟡 Should Fix | ✅ แก้แล้ว v002 |
| 5 | `onOpen` | เพิ่ม Quick Action ที่ Root Menu | 🟡 Should Fix | ✅ แก้แล้ว v002 |
| 6 | `runFullPipeline` | เพิ่ม **LockService** กัน double-click | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 7 | `checkSystemIntegrity` | เพิ่มปุ่ม shortcut ไป `setupAllSheets` เมื่อพบชีตหาย | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |
| 8 | `showVersionInfo` | เปลี่ยนเป็น multiline Template Literal | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |
| 9 | `onOpen` | เรียก `validateConfig()` จาก 01\_Config | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล |
| ----- | ----- | ----- |
| `01_Config.gs` | `validateConfig()` | ต้องถูกเรียกจาก `onOpen()` ใน `00_App.gs` — ปัจจุบันยังไม่ได้เชื่อมกัน |

---

## **📋 Module 01\_Config.gs — ผล Review**

---

## **1️⃣ ตรวจสอบข้อเท็จจริง**

ฐานการตรวจ: `01_Config.gs v001` (ที่ AI ท่านต่างๆ ได้รับไปวิเคราะห์) เทียบกับ `v002` ที่เราแก้ไปแล้ว

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `THRESHOLD_IGNORE = 70` ทับกับ `THRESHOLD_REVIEW` | ✅ **ถูกต้อง** | Bug จริง — แก้แล้วใน v002 เป็น `50` |
| `\\n` ใน `getGeminiApiKey` | ✅ **ถูกต้อง** | แก้แล้วใน v002 เป็น `\n` |
| `API_URL` Hardcode | ✅ **ถูกต้อง** | แก้แล้วใน v002 ใช้ `PropertiesService` fallback |
| Column Index 0-based อ่านยาก แนะนำ Reverse Map | 🟡 **Nice-to-have** | ไม่ใช่ Bug — เป็นแนวคิดเสริม Debug |
| ไม่มี `SCHEMA_VERSION` | ✅ **ถูกต้อง** | แก้แล้วใน v002 เพิ่ม `const SCHEMA_VERSION = '1.0.0'` |
| `SHEET.SOURCE` ชื่อไทยยาว ควรเปลี่ยน | ❌ **เข้าใจผิด** | ห้ามเปลี่ยน — ต้องตรงกับชื่อชีตจริงในระบบ SCG |
| ไม่มี `Object.freeze()` | ✅ **ถูกต้อง** | แก้แล้วใน v002 freeze ทุก Object |
| เพิ่ม `validateConfig()` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| เพิ่ม Config Snapshot Log ทุกครั้งที่ run pipeline | 🟡 **Nice-to-have** | ยังไม่ได้ทำ |

---

### **🤖 Gemini / AIStudio (ท่านที่ 1\)**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `THRESHOLD_IGNORE = THRESHOLD_REVIEW = 70` Logic ทับกัน | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| `\\n` ใน `getGeminiApiKey` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| `API_URL` ควร fallback จาก Properties | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| ขาด `SCHEMA_VERSION` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| ขาด `Object.freeze()` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| ขาด `validateConfig()` ตอน startup | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| แนะนำ Auto Check Column Length | 🟡 **Nice-to-have** | ยังไม่ได้ทำ — เกี่ยวกับ `03_SetupSheets.gs` |
| แนะนำ Config Snapshot Log | 🟡 **Nice-to-have** | ยังไม่ได้ทำ |

---

### **🤖 Gemini / AIStudio (ท่านที่ 2\)**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `\\n` ใน `getGeminiApiKey` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| Index ถูกต้อง 0-based ทุกตัว | ✅ **ถูกต้อง** | ยืนยันครับ |
| ไม่มี hardcode API Key | ✅ **ถูกต้อง** | ใช้ `PropertiesService` ถูกต้อง |
| `REVIEW_IDX` comment อ้างอิง GitHub | 🟡 **Nice-to-have** | ไม่กระทบ Logic |
| `SYS_TH_GEO` ยังไม่มี Index Object | ✅ **สังเกตถูก** | `SYS_TH_GEO` Index อยู่ใน `16_GeoDictionaryBuilder.gs` เป็น `TH_GEO_IDX` — ถูกต้องแล้ว แยกไฟล์ไว้ |

---

### **🤖 Perplexity / Claude**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `\\n` ใน `getGeminiApiKey` (v001) | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| โครงสร้างดี ไม่มี Bug อื่น | ✅ **ถูกต้อง** | ยืนยัน |
| `REVIEW_IDX` comment GitHub ควรย้ายไป docstring | 🟢 **Minor** | ไม่กระทบ Logic |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 01**

| \# | ฟังก์ชัน/ส่วน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `AI_CONFIG` | `THRESHOLD_IGNORE`: 70→50 | 🔴 Critical | ✅ แก้แล้ว v002 |
| 2 | `getGeminiApiKey()` | `\\n` → `\n` | 🔴 Critical | ✅ แก้แล้ว v002 |
| 3 | `SCG_CONFIG.API_URL` | Hardcode → `PropertiesService` fallback | 🟡 Should Fix | ✅ แก้แล้ว v002 |
| 4 | ทุก Config Object | เพิ่ม `Object.freeze()` | 🟡 Should Fix | ✅ แก้แล้ว v002 |
| 5 | Global | เพิ่ม `SCHEMA_VERSION` | 🟡 Should Fix | ✅ แก้แล้ว v002 |
| 6 | `validateConfig()` | เพิ่มฟังก์ชันตรวจ Threshold \+ Schema | 🟡 Should Fix | ✅ แก้แล้ว v002 |
| 7 | `AI_CONFIG` | เพิ่ม Config Snapshot Log ตอน run pipeline | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |
| 8 | `*_IDX` ทุกตัว | เพิ่ม Reverse Map เพื่อ Debug | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล |
| ----- | ----- | ----- |
| `00_App.gs` | `onOpen()` | ต้องเรียก `validateConfig()` จาก 01\_Config — ยังไม่ได้เชื่อมกัน |
| `00_App.gs` | `runFullPipeline()` | ควร log `AI_CONFIG` snapshot ก่อนรัน Pipeline (Nice-to-have) |

---

## **📋 Module 02\_Schema.gs — ผล Review จาก 7 AI**

---

## **1️⃣ ตรวจสอบข้อเท็จจริง**

ฐานการตรวจ: `02_Schema.gs v001` (ที่ AI ได้รับ) เทียบกับ `v002` ที่เราแก้แล้ว

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SHIPMENT_SUMMARY` key ไม่ตรงกับ `SHEET.SHIPMENT_SUM` | ✅ **ถูกต้อง** | Bug จริง — แก้แล้วใน v002 เปลี่ยน key เป็น `'สรุป_Shipment'` |
| `validateSheetHeaders` ไม่เช็คลำดับ Column | ✅ **ถูกต้อง** | แก้แล้วใน v002 เพิ่ม `wrongOrder` check |
| Header เปรียบเทียบแบบ case-sensitive | ✅ **ถูกต้อง** | แก้แล้วใน v002 เพิ่ม `normalize = toLowerCase()` |
| `SCHEMA` ไม่มี `Object.freeze()` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| `getColIndex` O(n) ทุกครั้ง ควร Pre-build Map | 🟡 **Nice-to-have** | ยังไม่ได้ทำ |
| ไม่มี Schema version check | ✅ **ถูกต้อง** | แก้แล้วใน v002 ด้วย `validateSchemaConsistency()` |
| ไม่มี Duplicate Header check | 🟡 **Nice-to-have** | ยังไม่ได้ทำ |
| Auto Fix Header / Lock Schema / Auto Create | 🟢 **Nice-to-have** | `createSheetIfMissing_` ใน 03 ทำอยู่แล้ว |

---

### **🤖 Gemini/Qwen**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SCHEMA` key ไม่ตรงกับ `SHEET.OWNER_SUMMARY` / `SHIPMENT_SUM` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| `raw_long` → `raw_lng` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| `destination_id` vs `dest_id` | ✅ **ถูกต้อง** | แก้แล้วใน v002 เปลี่ยนเป็น `dest_id` |
| `RPT_DATA_QUALITY` vs `RPT_QUALITY` | ✅ **ถูกต้อง** | แก้แล้วใน v002 — SCHEMA key เป็น `'RPT_DATA_QUALITY'` ตรงกับ `SHEET.RPT_QUALITY = 'RPT_DATA_QUALITY'` |
| `SYS_TH_GEO` ไม่ได้ validate ใน `00_App.gs` | ✅ **สังเกตถูก** | ยังไม่ได้เพิ่มใน `requiredSheets` ของ `checkSystemIntegrity` |
| `M_DESTINATION` มี `lat/lng` ซ้ำกับ `M_GEO_POINT` | ❌ **เข้าใจผิด** | ตั้งใจ Denormalize เพื่อ Performance — Module 17 ต้องการ lat/lng โดยตรงไม่ต้อง JOIN ทุกครั้ง |
| `validateSheetHeaders` case-sensitive | ✅ **ถูกต้อง** | แก้แล้วใน v002 |

---

### **🤖 Grok & xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `raw_long` → `raw_lng` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| `RPT_DATA_QUALITY` key ไม่ตรงกับ `RPT_QUALITY` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| ส่วนอื่นถูกต้อง ไม่มี syntax error | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `raw_long` → `raw_lng` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| Comment กลุ่ม Fact Table ระบุว่า "กลุ่ม 1" ทั้งหมดแต่ควรแยกเป็น กลุ่ม 2/3/4 | ✅ **ถูกต้อง** | ยังไม่ได้แก้ใน v002 |
| `DAILY_JOB` คอลัมน์ภาษาไทยใช้ underscore (`_`) ต้องตรงกับชีตจริง | ✅ **สังเกตถูก** | ต้องยืนยันกับชีตจริงในระบบ SCG |
| โครงสร้าง SCHEMA ถูกต้อง 100% ไม่มี syntax error | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 GenSpark**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `raw_long` → `raw_lng` | ✅ **ถูกต้อง** | แก้แล้วใน v002 |
| `validateSheetHeaders` ไม่เช็คลำดับ Column | ✅ **ถูกต้อง** | แก้แล้วใน v002 เพิ่ม `wrongOrder` |
| `isValid` ควรรวม `extra.length === 0` ด้วย | ❌ **เข้าใจผิด** | ไม่ถูกต้องสำหรับระบบนี้ เพราะ \[RULE 2\] อนุญาตให้เพิ่มคอลัมน์ต่อท้ายได้ ถ้าใส่ extra ใน isValid จะทำให้ PATCH header พัง |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 02**

| \# | ฟังก์ชัน/ส่วน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `SCHEMA` keys | `SHIPMENT_SUMMARY` → `'สรุป_Shipment'`, `OWNER_SUMMARY` → `'สรุป_เจ้าของสินค้า'` | 🔴 Critical | ✅ แก้แล้ว v002 |
| 2 | `Q_REVIEW` \[11\] | `raw_long` → `raw_lng` | 🔴 Critical | ✅ แก้แล้ว v002 |
| 3 | `FACT_DELIVERY` \[18\] | `destination_id` → `dest_id` | 🔴 Critical | ✅ แก้แล้ว v002 |
| 4 | `SCHEMA` key | `RPT_DATA_QUALITY` ให้ตรงกับ `SHEET.RPT_QUALITY` | 🔴 Critical | ✅ แก้แล้ว v002 |
| 5 | `validateSheetHeaders()` | เพิ่ม `wrongOrder` check \+ normalize toLowerCase | 🟡 Should Fix | ✅ แก้แล้ว v002 |
| 6 | `SCHEMA` | เพิ่ม `Object.freeze()` | 🟡 Should Fix | ✅ แก้แล้ว v002 |
| 7 | `validateSchemaConsistency()` | เพิ่มฟังก์ชันตรวจ SCHEMA length vs IDX keys | 🟡 Should Fix | ✅ แก้แล้ว v002 |
| 8 | Section Comments | แก้ comment "กลุ่ม 1" ให้แยกเป็น กลุ่ม 1/2/3/4 ตามประเภท | 🟢 Minor | ❌ ยังไม่ได้ทำ |
| 9 | `validateSheetHeaders()` | เพิ่ม Duplicate Header check | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |
| 10 | `getColIndex()` | Pre-build `SCHEMA_INDEX_MAP` แทน O(n) search | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |
| 11 | `DAILY_JOB` | ยืนยัน underscore `_` กับ space ในชีตจริง | 🟡 Should Verify | ⚠️ รอยืนยัน |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| `00_App.gs` | `checkSystemIntegrity()` | `requiredSheets` ยังขาด `SHEET.SYS_TH_GEO` | ❌ ต้องเพิ่ม |

---

## **📋 Module 03\_SetupSheets.gs — ผล Review จาก 6 AI**

---

## **1️⃣ ตรวจสอบข้อเท็จจริง**

ฐานการตรวจ: `03_SetupSheets.gs v001` เทียบกับโค้ดจริงและ v002 ของไฟล์ก่อนหน้า

**ข้อมูลสำคัญก่อนวิเคราะห์:**

| ค่าใน 01\_Config v002 | ค่าจริง |
| ----- | ----- |
| `SHEET.RPT_QUALITY` | `= 'RPT_DATA_QUALITY'` |
| `SHEET.SHIPMENT_SUM` | `= 'สรุป_Shipment'` |
| `SHEET.OWNER_SUMMARY` | `= 'สรุป_เจ้าของสินค้า'` |
| `SCHEMA` key หลัง v002 | เปลี่ยนเป็นชื่อชีตจริงทั้งหมด |

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SCHEMA.SHIPMENT_SUMMARY` ไม่ตรง key | ✅ **ถูกต้อง** | Bug จริง — หลัง 02 v002 เปลี่ยน key เป็น `'สรุป_Shipment'` ทำให้ `SCHEMA.SHIPMENT_SUMMARY` \= undefined |
| `appendMissingHeaders_` ไม่เช็คลำดับ | ❌ **เข้าใจผิด** | ตั้งใจแล้ว — \[RULE 2\] เพิ่มต่อท้ายเท่านั้น การ reorder ไม่ใช่หน้าที่ของฟังก์ชันนี้ |
| Logging `appendRow` ช้าเมื่อ scale | 🟡 **Nice-to-have** | ยังไม่ได้ทำ |
| ไม่มี `LockService` กัน setup ซ้ำ | 🟡 **Should Fix** | ยังไม่ได้ทำ |
| `setColumnWidths` ทุกครั้งแม้ PATCHED | 🟢 **Minor** | ยังไม่ได้ทำ |
| Dropdown `maxRows = 1000` แข็ง | 🟡 **Should Fix** | ยังไม่ได้ทำ |
| `clearOldLogs` ลบทีละแถวใน Loop ช้า | 🟡 **Should Fix** | ยังไม่ได้ทำ |

---

### **🤖 Gemini**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SHEET.RPT_QUALITY` vs `SCHEMA.RPT_DATA_QUALITY` | ❌ **เข้าใจผิด** | `SHEET.RPT_QUALITY = 'RPT_DATA_QUALITY'` และ SCHEMA key คือ `'RPT_DATA_QUALITY'` — `SCHEMA.RPT_DATA_QUALITY` จึงถูกต้อง |
| `SCHEMA.SHIPMENT_SUMMARY` ไม่ตรง | ✅ **ถูกต้อง** | Bug จริง — เหตุผลเดียวกับ ChatGPT |
| `REVIEW_IDX` ไม่ได้ประกาศในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope — `REVIEW_IDX` อยู่ใน `01_Config.gs` ใช้ได้ทันที |
| `clearOldLogs` loop ช้า | ✅ **ถูกต้อง** | ควรแก้ |
| Dropdown 1000 rows | ✅ **ถูกต้อง** | ควรแก้เป็น dynamic |
| สีชีตซ้ำกัน | 🟢 **Minor** | ไม่ผิด แค่ UX |

---

### **🤖 GLMz**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SHEET.RPT_QUALITY` \+ `SCHEMA.RPT_DATA_QUALITY` — วินิจฉัยว่า key ไม่ตรง | ❌ **เข้าใจผิด** | ตรงกันแล้ว เพราะ `SHEET.RPT_QUALITY = 'RPT_DATA_QUALITY'` |
| `SCHEMA.SHIPMENT_SUMMARY` ไม่ตรง | ✅ **ถูกต้อง** | Bug จริง |
| `clearOldLogs` loop ช้า | ✅ **ถูกต้อง** | ควรแก้ |

---

### **🤖 Grok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SHEET.SHIPMENT_SUM` \+ `SCHEMA.SHIPMENT_SUMMARY` ไม่ตรง | ✅ **ถูกต้อง** | Bug จริง |
| `REVIEW_IDX` ไม่ได้ประกาศในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SHEET.RPT_QUALITY` vs Schema ไม่ตรง | ❌ **เข้าใจผิด** | เหตุผลเดียวกับ GLMz |
| `SCHEMA.SHIPMENT_SUMMARY` ไม่ตรง | ✅ **ถูกต้อง** | Bug จริง |
| `REVIEW_IDX` ไม่ได้ประกาศ | ❌ **เข้าใจผิด** | GAS shared scope |
| `SHEET.OWNER_SUMMARY`, `SHEET.SYS_TH_GEO` ต้องมีใน Config | ❌ **เข้าใจผิด** | มีใน `01_Config.gs` v002 แล้วทั้งคู่ |
| แนะนำ Hardcode index ชั่วคราว | ❌ **ไม่ถูก** | ละเมิด \[RULE 3\] — ต้องใช้ `REVIEW_IDX` จาก Config เท่านั้น |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SHEET.SHIPMENT_SUM` กับ `SCHEMA.SHIPMENT_SUMMARY` อาจไม่ตรง | ✅ **ถูกต้อง** | Bug จริง |
| `REVIEW_IDX` ต้องมีประกาศใน `01_Config.gs` | ✅ **สังเกตถูก** | มีอยู่แล้วใน `01_Config.gs` — GAS shared scope ใช้ได้ |
| โครงสร้างถูกต้องทุกอย่าง | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| ไม่พบจุดผิดเลย | ✅ **ถูกต้อง (บางส่วน)** | ถูกที่ว่าไม่มี syntax error แต่มี Bug จาก key mismatch หลัง 02 v002 |
| Backward Loop ใน `clearOldLogs` ดีมาก | ✅ **ถูกต้อง** | Logic ถูก แต่ยังช้าเมื่อ scale ใหญ่ |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 03**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `setupGroupTwoSheets_()` | `SCHEMA.SHIPMENT_SUMMARY` → `SCHEMA[SHEET.SHIPMENT_SUM]` หรือ `getSheetHeaders(SHEET.SHIPMENT_SUM)` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 2 | `setupGroupTwoSheets_()` | `SCHEMA.OWNER_SUMMARY` → `SCHEMA[SHEET.OWNER_SUMMARY]` หรือ `getSheetHeaders(SHEET.OWNER_SUMMARY)` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 3 | ทุก `createSheetIfMissing_` call | เปลี่ยน Pattern เป็น `getSheetHeaders(SHEET.xxx)` แทน `SCHEMA.xxx` เพื่อรองรับ key ภาษาไทย | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 4 | `setupReviewDropdowns_()` | `maxRows = 1000` → `sheet.getMaxRows() - 1` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 5 | `clearOldLogs()` | เปลี่ยนจาก `deleteRow` ทีละแถว → filter แล้ว batch rewrite | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 6 | `setupAllSheets()` | เพิ่ม `LockService` กัน setup ซ้ำซ้อน | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 7 | `writeHeaderRow_()` | `setColumnWidths` ทำเฉพาะ `CREATED` ไม่ทำตอน `PATCHED` | 🟢 Minor | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| `00_App.gs` | `checkSystemIntegrity()` | `requiredSheets` ยังขาด `SHEET.SYS_TH_GEO` (พบจาก Module 02 Review ด้วย) | ❌ ต้องเพิ่ม |
| `02_Schema.gs` | Pattern ทุก call | หลัง v002 เปลี่ยน SCHEMA key เป็นชื่อชีตภาษาไทย → ต้องใช้ `SCHEMA[SHEET.xxx]` หรือ `getSheetHeaders()` แทน dot notation | ❌ กระทบ 03 โดยตรง |

---

## **📋 Module 04\_SourceRepository.gs — ผล Review จาก 6 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `parseFloat() || 0` กลบ NaN และ `hasGeo` ไม่กัน NaN | ✅ **ถูกต้อง** | Bug จริง — `NaN !== 0` \= true ทำให้ระบบคิดว่ามีพิกัดทั้งที่ไม่มี |
| Cache เต็มควร `logWarn` ไม่ใช่ silent catch | ✅ **ถูกต้อง** | ปัจจุบัน catch เงียบ debug ยาก |
| `processSrcBatch_` catch ทั้ง batch ทำให้ row เดียวพัง \= หยุดทั้งก้อน | ✅ **ถูกต้อง** | Bug จริง — ควรแยก try-catch ระดับ row |
| `ROW_NUMBER: 0` ประกาศแต่ไม่ได้ใช้ | ✅ **สังเกตถูก** | Minor — ประกาศเพื่อ Schema reference แต่ไม่ได้ใช้จริง |
| `String().trim()` ซ้ำซ้อน ควรทำ helper | 🟢 **Nice-to-have** | ยังไม่ได้ทำ |
| `getLastColumn()` เรียกซ้ำ | 🟢 **Minor** | ยังไม่ได้ทำ |
| Cache key hardcode ควรเป็น constant | 🟢 **Nice-to-have** | ยังไม่ได้ทำ |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `loaded++` นับก่อนรู้ผลจาก `processSrcBatch_` จริง | ✅ **ถูกต้อง** | ตัวเลข "โหลด" ไม่ตรงกับของที่ประมวลผลสำเร็จจริง |
| `processSrcBatch_` catch ทั้ง batch เงียบ | ✅ **ถูกต้อง** | Bug จริง |
| Date Object กลายเป็น String หลังผ่าน JSON cache | ✅ **ถูกต้อง** | ควร serialize เป็น ISO String ก่อน cache |
| `AI_CONFIG`, `FACT_IDX`, `processOneRow` ไม่มีในไฟล์ | ❌ **เข้าใจผิด** | GAS shared scope — ทั้งหมดอยู่ใน `01_Config.gs` / `10_MatchEngine.gs` ใช้ได้ทันที |
| อ่าน `getLastColumn()` เกินคอลัมน์จำเป็น | 🟡 **Should Fix** | ควรอ่านแค่ `Math.max(...Object.values(SRC_IDX)) + 1` |

---

### **🤖 GLMz**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| ไม่มีข้อผิดพลาดเลย 100% | ✅ **ถูกต้องบางส่วน** | Syntax ถูก แต่มี Logic Bug เรื่อง NaN และ batch error ที่ควรแก้ |
| `parseFloat() || 0` ป้องกัน NaN ได้แล้ว | ❌ **เข้าใจผิด** | `parseFloat('abc') || 0` \= 0 แต่ `hasGeo` ใช้ `rawLat !== 0` ซึ่ง NaN ผ่านได้ |

---

### **🤖 Grok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `AI_CONFIG` ไม่ได้ประกาศในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope — อยู่ใน `01_Config.gs` v002 แล้ว |
| `processOneRow` ไม่ได้ define ในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope — อยู่ใน `10_MatchEngine.gs` |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SHEET.SOURCE`, `FACT_IDX`, `AI_CONFIG` ไม่มีในไฟล์ | ❌ **เข้าใจผิด** | GAS shared scope — มีทั้งหมดแล้วใน `01_Config.gs` |
| `processOneRow` ไม่ได้ define | ❌ **เข้าใจผิด** | GAS shared scope — อยู่ใน `10_MatchEngine.gs` |
| `AI_CONFIG.BATCH_SIZE * 5` Batch ใหญ่เกิน | 🟢 **Minor** | ตั้งใจให้ Batch ใหญ่ขึ้น 5x เพื่อลด API call ไม่ใช่ Bug |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `AI_CONFIG` ต้องมีประกาศใน 01\_Config | ❌ **เข้าใจผิด** | มีอยู่แล้วใน `01_Config.gs` v002 ครบถ้วน |
| Logic ถูกต้องทั้งหมด | ✅ **ถูกต้องบางส่วน** | Syntax ถูก แต่ยังมี NaN bug และ batch error |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| ไม่มีข้อผิดพลาดเลย Perfect | ✅ **ถูกต้องบางส่วน** | Syntax ถูก แต่มี NaN bug และ batch error ที่ควรแก้ |
| Cache try-catch ดีมาก | ✅ **ถูกต้อง** | แต่ควรเพิ่ม `logWarn` เมื่อ cache ล้มเหลว |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 04**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `buildSourceObj_()` | `parseFloat() || 0` → `Number()` \+ `isNaN()` check และ `hasGeo` เพิ่ม `!isNaN()` guard | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 2 | `processSrcBatch_()` | แยก try-catch ระดับ row แทน catch ทั้ง batch — ป้องกัน data loss เงียบ | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 3 | `runLoadSource()` | `loaded++` ย้ายไปนับหลัง process จริง ไม่ใช่ก่อน | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 4 | `buildSourceObj_()` | `deliveryDate` → แปลงเป็น ISO String ก่อน cache ป้องกัน Date → String ผ่าน JSON | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 5 | `getAllSourceRows()` | เพิ่ม `logWarn` ใน catch block เมื่อ Cache เต็ม | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 6 | `getAllSourceRows()` | อ่านแค่คอลัมน์ที่จำเป็น `Math.max(...Object.values(SRC_IDX)) + 1` แทน `getLastColumn()` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 7 | `SRC_IDX` | `ROW_NUMBER: 0` ประกาศแต่ไม่ใช้ — ลบหรือใช้จริง | 🟢 Minor | ❌ ยังไม่ได้ทำ |
| 8 | Cache keys | เปลี่ยน hardcode string เป็น constant | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

ไม่มีผลกระทบต่อ Module 00–03 ที่วิเคราะห์ไปแล้วครับ

---

## **📋 Module 05\_NormalizeService.gs — ผล Review จาก 6 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| Regex `\u0E30-\u0E4E` \+ `\u0E47-\u0E4E` ซ้อนกัน | ✅ **ถูกต้อง** | Bug จริง — `\u0E47` อยู่ในช่วง `\u0E30-\u0E4E` อยู่แล้ว range ที่ 2 ซ้ำฟรี |
| `replace(noteWord, '')` ลบแค่ตัวแรก ไม่ใช่ global | ✅ **ถูกต้อง** | Bug จริง — ควรใช้ `new RegExp(noteWord, 'g')` |
| `replace(suffix, '')` ลบแค่ตัวแรก ไม่ใช่ global | ✅ **ถูกต้อง** | Bug จริง — เหตุผลเดียวกัน |
| `COMPANY_SUFFIX_LIST.some(s => working.includes(s))` หลวม | ✅ **ถูกต้อง** | "ร้านลุงจำกัดความอร่อย" จะถูก detect เป็น company ผิด |
| Prefix ตัดแค่ 1 ชั้น (break) | ✅ **ถูกต้อง** | "ดร.นาย" จะเหลือ "นายสมชาย" — ควรวน while loop |
| Regex `[^\u0E00-\u0E7F...]` ลบวงเล็บ/& | ✅ **สังเกตถูก** | ใช้กับชื่อคนได้ แต่ถ้าเผลอใช้กับ address จะพัง |
| `PHONE_PATTERN` ไม่ strict | 🟢 **Minor** | ใช้งานได้แต่อาจจับเบอร์แปลก |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| ลำดับ COMPANY\_SUFFIX ตัดก่อน อาจเปลี่ยนชื่อคนผิด | ✅ **ถูกต้อง** | "จำกัด" ถูกตัดก่อน "ห้างหุ้นส่วนจำกัด" เพราะไม่ได้ sort longest-first |
| Prefix "พลเอก" ถูกตัดหมดเหลือว่าง | 🟡 **Should Fix** | ควรเช็คว่าหลังตัดแล้วเหลืออะไรก่อน |
| `DELIVERY_NOTE_LIST` trim หลัง loop | ✅ **ถูกต้อง** | ปัจจุบัน trim ทุกรอบอยู่แล้ว ไม่มีปัญหา |
| `PHONE_PATTERN` ปรับให้แม่นขึ้น | 🟢 **Nice-to-have** | ยังไม่ได้ทำ |
| Regex ขั้น 6 ลบ `()&,/` | ✅ **สังเกตถูก** | เหตุผลเดียวกับ ChatGPT |

---

### **🤖 Grok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `runNormalize()` ว่างเปล่า ไม่ทำงานจริง | ✅ **ถูกต้อง** | แต่ตั้งใจแล้ว — Normalize เกิดใน `processOneRow()` ใน 10\_MatchEngine ต่อรอบ ไม่ใช่ Batch แยก อย่างไรก็ตามฟังก์ชันควรมี comment อธิบายให้ชัดกว่านี้ |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `runNormalize()` ว่างเปล่า | ✅ **ถูกต้อง (บางส่วน)** | ตั้งใจแล้ว แต่ควรมี comment ชัดเจน |
| `replace(suffix, '')` ไม่ global | ✅ **ถูกต้อง** | Bug จริง |
| `logInfo` ไม่มีในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope — อยู่ใน `03_SetupSheets.gs` |
| `buildThaiPhoneticKey` Unicode range ไม่ครบ | ✅ **ถูกต้อง** | แต่ solution ที่แนะนำผิด — เอา `[\u0E00-\u0E7F]` ออกหมดจะเหลือแค่ภาษาอังกฤษ ไม่ใช่พยัญชนะไทย |
| ลำดับการตัดควร Suffix ก่อน Note | 🟡 **Nice-to-have** | บางเคสสมเหตุสมผล แต่ไม่ใช่ Bug |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `'ดร.'` ควรอยู่ก่อน `'ดร'` ในลิสต์ | ✅ **ถูกต้อง** | แม้ sort longest-first จะจัดการได้ แต่เพื่อความชัดเจนควรจัดลำดับในลิสต์ด้วย |
| `normalizePlaceName` regex `บ้าน` จับผิด | ✅ **ถูกต้อง** | "บ้านโป่ง" จะถูก classify เป็น house ผิด |
| Logic ถูกต้องทั้งหมด | ✅ **ถูกต้องบางส่วน** | Syntax ถูก แต่มี Bug เรื่อง global replace และ Regex overlap |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `COMPANY_SUFFIX_LIST` ไม่ sort longest-first | ✅ **ถูกต้อง** | Bug จริง — "จำกัด" ตัดก่อน "ห้างหุ้นส่วนจำกัด" ทำให้ตัดไม่สะอาด |
| `extractedDoc` ใน REF\_NO\_PATTERN ไม่ต่อข้อมูลถ้า docMatches มีแล้ว | ✅ **ถูกต้อง** | Bug จริง — REF ถูกลบออกจาก working แต่ไม่ได้ append เข้า extractedDoc |
| Regex `\u0E47-\u0E4E` ซ้อนกับ `\u0E30-\u0E4E` | ✅ **ถูกต้อง** | Bug จริง — ยืนยันซ้ำกับ ChatGPT |
| `sortedPrefix` ควร pre-sort เป็น Global ไม่ sort ทุก call | ✅ **ถูกต้อง** | ทุกครั้งที่เรียก `normalizePersonNameFull` จะ sort Array ใหม่ทั้งหมด |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 05**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `buildThaiPhoneticKey()` | Regex `\u0E47-\u0E4E` ซ้อนกับ `\u0E30-\u0E4E` → ลดเป็น `[\u0E30-\u0E4E\s]` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 2 | `normalizePersonNameFull()` ขั้นตอน 3 | `working.replace(noteWord, '')` → `new RegExp(noteWord, 'g')` ทุกจุด | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 3 | `normalizePersonNameFull()` ขั้นตอน 4 | `working.replace(suffix, '')` → `new RegExp(escapedSuffix, 'gi')` ทุกจุด | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 4 | `COMPANY_SUFFIX_LIST` | เพิ่ม sort longest-first เหมือน `PERSON_PREFIX_LIST` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 5 | `normalizePersonNameFull()` ขั้นตอน 2 | `extractedDoc` ใน REF block ควร append (`+=`) แทน skip เมื่อมี docMatches แล้ว | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 6 | `normalizePersonNameFull()` ขั้นตอน 5 | ตัด prefix แบบ while loop แทน break ครั้งเดียว | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 7 | Global | Pre-sort `PERSON_PREFIX_LIST` เป็น `SORTED_PREFIX_LIST` constant แทน sort ทุก call | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 8 | `runNormalize()` | เพิ่ม comment อธิบายชัดว่า Normalize เกิดใน `processOneRow()` ไม่ใช่ batch แยก | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 9 | `normalizePlaceName()` | Regex `บ้าน` → ปรับเป็น \`(^บ้าน\\s | หมู่บ้าน)\` กัน false positive | 🟡 Should Fix |
| 10 | `COMPANY_SUFFIX_LIST` detect | `.some(s => working.includes(s))` → เพิ่ม word boundary check | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 11 | `'ดร.'` / `'ดร'` | จัดลำดับในลิสต์ให้ `'ดร.'` อยู่ก่อน `'ดร'` เพื่อความชัดเจน | 🟢 Minor | ❌ ยังไม่ได้ทำ |
| 12 | `normalizePlaceName()` | `'ห้าง'` ใน Regex อาจ false positive — พิจารณาเปลี่ยนเป็น `ห้างสรรพสินค้า` เท่านั้น | 🟢 Minor | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

ไม่มีผลกระทบต่อ Module 00–04 ที่วิเคราะห์ไปแล้วครับ

---

## **📋 Module 06\_PersonService.gs — ผล Review จาก 5 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| Comment สะกดผิด "ห้างลบ" → "ห้ามลบ" | ✅ **ถูกต้อง** | พิมพ์ผิดจริงใน `mergePersonRecords` |
| `createPersonAlias(targetId, sourceId, 100)` ใส่ ID แทนชื่อ | ✅ **ถูกต้อง** | Bug จริง — `aliasName` ควรเป็น canonical name ไม่ใช่ person\_id |
| `updatePersonStats` โหลดทั้งชีตขัด RULE 6 | ✅ **ถูกต้อง** | Comment บอก "อ่านแถวเดียว" แต่โค้ดโหลด `getLastColumn()` ทั้งหมด |
| `headers.indexOf()` ไม่มี guard ถ้าได้ \-1 | ✅ **ถูกต้อง** | `data[i][-1]` \= undefined พังเงียบ |
| `results.includes(found)` compare object reference | ✅ **ถูกต้อง** | Bug จริง — ถ้า object คนละ instance จะซ้ำได้ |
| Phonetic fallback ใช้ 2 ตัวอักษรแรก false positive เยอะ | ✅ **ถูกต้อง** | "สมชาย/สมศักดิ์/สมปอง" ชน 2 ตัวแรกหมด |
| Phone Match หยุดเลยแม้เบอร์ซ้ำหลายคน | ✅ **ถูกต้อง** | ควร continue scoring ถ้า byPhone.length \> 1 |
| `scorePersonCandidate` hardcode `60` แทน Config | ✅ **ถูกต้อง** | ควรใช้ `AI_CONFIG.MIN_SCORE` หรือ constant |
| `createPerson` normalized \= cleanName ไม่ผ่าน `normalizeForCompare` | 🟡 **Nice-to-have** | ไม่ใช่ Bug แต่ normalized ควรเป็นรูปแบบที่ใช้เปรียบเทียบ |
| `toLocaleDateString('th-TH')` timezone อาจเพี้ยน | 🟢 **Minor** | GAS timezone ขึ้นกับ Project settings |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `updatePersonStats` data\[i\] offset อาจคลาดเคลื่อน | ❌ **เข้าใจผิด** | โค้ดใช้ `data[0]` เป็น header และ loop จาก `i=1` ถูกต้องแล้ว `targetRow = i + 1` ถูกต้อง |
| `mergePersonRecords` ย้าย idCol/statCol/noteCol ออกนอก loop | ❌ **เข้าใจผิด** | ดูโค้ดจริง — `idCol`, `statCol`, `noteCol` ประกาศนอก loop อยู่แล้ว |
| `results.includes(found)` object reference | ✅ **ถูกต้อง** | Bug จริง |
| Phone prefix `'` สอดคล้องกันระหว่าง create/load | ✅ **ถูกต้อง** | ยืนยัน — create ใส่ `'` และ load ลบ `'` ออก ถูกต้อง |

---

### **🤖 Grok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `levenshteinDistance` / `diceCoefficient` ไม่มีในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope — อยู่ใน `14_Utils.gs` ใช้ได้ทันที |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `AI_CONFIG`, `PERSON_IDX`, `APP_CONST` ไม่มีในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope — มีทั้งหมดใน `01_Config.gs` v002 |
| `levenshteinDistance` / `diceCoefficient` ไม่มี | ❌ **เข้าใจผิด** | GAS shared scope — อยู่ใน `14_Utils.gs` |
| `results.includes(found)` object reference | ✅ **ถูกต้อง** | Bug จริง |
| `loadAllPersons_` ควรใช้ `PERSON_IDX.RECORD_STATUS` ไม่ใช่ `PERSON_IDX.STATUS` | ❌ **เข้าใจผิด** | ใน `01_Config.gs` ของเรา `PERSON_IDX.STATUS = 7` คือ column `record_status` ถูกต้องแล้ว ไม่มี key ชื่อ `RECORD_STATUS` |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `createPersonAlias(targetId, sourceId, 100)` ผิดความหมาย | ✅ **ถูกต้อง** | Bug จริง — ควรส่ง canonical name ของ sourceId |
| `updatePersonStats` โหลดทั้งชีต อาจช้าอนาคต | ✅ **ถูกต้อง** | ควร optimize |
| `results.includes(found)` — ชมว่า "ดีแล้ว" | ❌ **เข้าใจผิด** | จริงๆ มี Bug เรื่อง object reference compare |
| ภาพรวม Logic ถูกต้องทั้งหมด | ✅ **ถูกต้องบางส่วน** | ส่วนใหญ่ถูก แต่มี Bug aliasName และ includes |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| Comment สะกดผิด "ห้างลบ" → "ห้ามลบ" | ✅ **ถูกต้อง** | พิมพ์ผิดจริง |
| ไม่มีข้อผิดพลาดอื่น Perfect | ✅ **ถูกต้องบางส่วน** | Syntax ถูก แต่มี Bug `aliasName` และ `includes` ที่ควรแก้ |
| `results.includes()` ชมว่าดี | ❌ **เข้าใจผิด** | มี Bug object reference compare |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 06**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `mergePersonRecords()` | `createPersonAlias(targetId, sourceId, 100)` → ดึง `canonical` ของ sourceId มาใช้แทน `sourceId` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 2 | `findPersonCandidates()` | `results.includes(found)` → `!results.some(p => p.personId === found.personId)` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 3 | `findByAlias_()` | เหตุผลเดียวกัน — `found.push(alias[...])` แทน includes | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 4 | `updatePersonStats()` | โหลดเฉพาะ `person_id` column แทนทั้งชีต \+ เพิ่ม guard ถ้า `idCol === -1` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 5 | `mergePersonRecords()` | เพิ่ม guard `if (idCol === -1)` ก่อนใช้ | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 6 | `findPersonCandidates()` | Phonetic fallback เปลี่ยน `substring(0, 2)` → `substring(0, 3)` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 7 | `findPersonCandidates()` | Phone match — ถ้า `byPhone.length > 1` ไม่ return ทันที ให้ไปต่อ scoring | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 8 | `scorePersonCandidate()` | Hardcode `60` → ใช้ `AI_CONFIG.THRESHOLD_IGNORE` หรือ constant ใหม่ | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 9 | `mergePersonRecords()` | Comment "ห้างลบ" → "ห้ามลบ" | 🟢 Minor | ❌ ยังไม่ได้ทำ |
| 10 | `createPerson()` | `normalized_name` ควรผ่าน `normalizeForCompare()` ก่อนเก็บ | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| `01_Config.gs` | `AI_CONFIG` | ควรเพิ่ม `SCORE_MIN_THRESHOLD: 60` เป็น constant แทน hardcode ใน Module 06 | ❌ ต้องเพิ่ม |

---

## **📋 Module 07\_PlaceService.gs — ผล Review จาก 5 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `results.includes(found)` / `results.includes(place)` — Object reference | ✅ **ถูกต้อง** | Bug จริง เหมือน Module 06 — ควรใช้ `.some(p => p.placeId === found.placeId)` |
| `extractProvince_` regex `\S+` ดูดเกิน | ✅ **ถูกต้อง** | "จ.ภูเก็ตต.ฉลอง" จะได้ "ภูเก็ตต.ฉลอง" — ควรใช้ `[ก-๙]+` |
| `findPlaceCandidates` fallback ไม่มี guard `normB` | ✅ **ถูกต้อง** | `''.startsWith()` \= false แต่ data dirty อาจเข้ามา |
| `tryMatchBranch` province condition หลวม | ✅ **ถูกต้อง** | \`\!province |
| `loadAllPlaces_` ไม่กรอง `STATUS_MERGED` | ✅ **ถูกต้อง** | Bug จริง — `loadAllPersons_` กรองทั้ง ARCHIVED และ MERGED แต่ `loadAllPlaces_` กรองแค่ ARCHIVED |
| `updatePlaceStats` `headers.indexOf()` ไม่มี guard | ✅ **ถูกต้อง** | ควรเพิ่ม `if (idCol === -1)` |
| `scorePlaceCandidate` hardcode `55` | ✅ **ถูกต้อง** | ควรใช้ Config constant |
| Branch Match score `75/85` ตายตัวเกิน | 🟢 **Nice-to-have** | ยังไม่ได้ทำ |
| `extractProvince_` ไม่รองรับ `อ.เมือง จ.ชลบุรี` | 🟡 **Should Fix** | ยังไม่ได้ทำ |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `updatePlaceStats` โหลดทั้งชีตขัด RULE 6 \+ ใช้ `headers.indexOf('place_id')` ขัด RULE 3 | ✅ **ถูกต้อง** | Bug จริง — ควรอ่านเฉพาะคอลัมน์ place\_id และใช้ `PLACE_IDX.PLACE_ID + 1` |
| ส่วนอื่นถูกต้อง | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 Grok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `levenshteinDistance` / `diceCoefficient` ไม่มีในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope — อยู่ใน `14_Utils.gs` |
| `CHAIN_STORE_LIST` ไม่ได้ declare ในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope — อยู่ใน `05_NormalizeService.gs` ใช้ได้ทันที |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `AI_CONFIG`, `PLACE_IDX`, `APP_CONST` ไม่มีในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope — มีทั้งหมดใน `01_Config.gs` |
| `levenshteinDistance`, `diceCoefficient` ไม่มี | ❌ **เข้าใจผิด** | GAS shared scope — `14_Utils.gs` |
| `CHAIN_STORE_LIST` ไม่มีในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope — `05_NormalizeService.gs` |
| `results.includes(found/place)` Object reference | ✅ **ถูกต้อง** | Bug จริง |
| `loadAllPlaces_` เรียกซ้ำหลายครั้ง ควร pass เป็น parameter | 🟡 **Nice-to-have** | Cache ช่วยลดได้ แต่ลด call ได้อีก |
| `normResult.notes` ควรเช็คว่าเป็น array ก่อน `.join()` | ✅ **ถูกต้อง** | ถ้า `normResult.notes` ไม่ได้มาจาก `normalizePlaceName` อาจ undefined |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `tryMatchBranch` province condition — \`\!province |  | \!p.province |
| `updatePlaceStats` โหลดทั้งชีต อาจช้า | ✅ **ถูกต้อง** | Performance issue |
| Logic ถูกต้องทั้งหมด | ✅ **ถูกต้องบางส่วน** | Syntax ถูก แต่มี Bug `includes`, `extractProvince_`, `loadAllPlaces_` filter |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `extractProvince_` regex `\S+` ดูดเกินเมื่อพิมพ์ติดกัน "จ.ภูเก็ตต.ฉลอง" | ✅ **ถูกต้อง** | Bug จริง — ยืนยันซ้ำกับ ChatGPT |
| `updatePlaceStats` ใช้ `headers.indexOf` dynamic หาแถว แต่ `PLACE_IDX.USAGE_COUNT` fixed เขียน — ย้อนแย้ง | ✅ **สังเกตถูก** | ตั้งใจแบบนี้ตาม RULE 2/3 แต่ควรทำให้ consistent ทิศทางเดียวกัน |
| ไม่มี Syntax error | ✅ **ถูกต้อง** | ยืนยัน |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 07**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `findPlaceCandidates()` | `results.includes(found)` → `.some(p => p.placeId === found.placeId)` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 2 | `findPlaceCandidates()` | `results.includes(place)` ใน allPlaces loop → `.some(p => p.placeId === place.placeId)` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 3 | `loadAllPlaces_()` | เพิ่ม filter `&& r[PLACE_IDX.STATUS] !== APP_CONST.STATUS_MERGED` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 4 | `extractProvince_()` | `\S+` → `[ก-๙]+` และเพิ่ม lookahead หยุดที่ \`อ. | ต. | เขต |
| 5 | `tryMatchBranch()` | Province condition: `!province || !p.province || p.province === province` → `!province || p.province === province` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 6 | `updatePlaceStats()` | โหลดเฉพาะ place\_id column \+ เพิ่ม `if (idCol === -1)` guard \+ ใช้ `PLACE_IDX` แทน `headers.indexOf` ตอน write | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 7 | `findPlaceCandidates()` | เพิ่ม `&& normB` guard ก่อน `normB.startsWith()` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 8 | `createPlace()` | `normResult.notes` ควรเช็ค Array.isArray() ก่อน `.join()` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 9 | `scorePlaceCandidate()` | Hardcode `55` → ใช้ Config constant | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 10 | `tryMatchBranch()` | Branch score `75/85` ตายตัว — พิจารณาใช้ `usageCount` ช่วย boost | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| `01_Config.gs` | `AI_CONFIG` | ควรเพิ่ม `PLACE_SCORE_MIN: 55` เป็น constant แทน hardcode | ❌ ต้องเพิ่ม |
| `06_PersonService.gs` | `loadAllPersons_()` | ยืนยันว่า filter ทั้ง ARCHIVED และ MERGED อยู่แล้ว ✅ ถูกต้อง — แต่ Module 07 ลืม filter MERGED | ❌ Module 07 ต้องเพิ่ม |

---

**พร้อ📋 Module 08\_GeoService.gs — ผล Review จาก 6 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`loadAllGeos_` ไม่กรอง `STATUS_MERGED`** | **✅ ถูกต้อง** | **Bug จริง — เหมือน Module 07** |
| **`headers.indexOf('geo_id')` ไม่มี guard** | **✅ ถูกต้อง** | **ควรเพิ่ม `if (idCol === -1)`** |
| **`resolveGeo` check \`\!lat** |  | **\!lng\` หลวม — string "13.7" ผ่านได้** |
| **Confidence formula อาจติดลบ** | **✅ ถูกต้อง** | **ควรเพิ่ม `Math.max(0, Math.min(100, ...))`** |
| **Grid 3×3 อาจ miss edge case** | **🟡 Should Fix** | **จริงแต่ radius=50m ปัจจุบันยังพอ** |
| **`buildGridKey_` ใช้ `Math.floor` bias ลงล่าง** | **✅ ถูกต้อง** | **แต่ทั้งสอง function ใช้เหมือนกัน ดังนั้น key ยังคงสอดคล้องกัน — ไม่ใช่ Bug แต่อาจ miss boundary** |
| **`createGeoPoint` ไม่ store gridKey ลง sheet** | **🟢 Nice-to-have** | **ปัจจุบันคำนวณทุกครั้งตอน load** |
| **Hardcode confidence \= 85** | **🟡 Should Fix** | **ควรแยกตาม source** |
| **`OUT_OF_BOUNDS` ไม่อยู่ใน JSDoc** | **✅ ถูกต้อง** | **Doc ไม่ครอบคลุม status นี้** |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **Grid Boundary 3×3 ครอบคลุมแล้ว (-1 ถึง 1\)** | **✅ ถูกต้อง** | **ยืนยัน — โค้ดทำ 9 ช่องอยู่แล้ว** |
| **Floating Point ใน Grid Key อาจทำให้ค้นหาไม่เจอ** | **✅ ถูกต้อง** | **จริง — `0.01 * 3 = 0.030000000000000002` ใน JS** |
| **`haversineDistanceM` ไม่มีในไฟล์นี้** | **❌ เข้าใจผิด** | **GAS shared scope — อยู่ใน `14_Utils.gs`** |
| **`updateGeoStats` โหลดทั้งชีต** | **✅ ถูกต้อง** | **ขัด RULE 6 เหมือน Module 06/07** |
| **JSDoc ไม่มี `OUT_OF_BOUNDS`** | **✅ ถูกต้อง** | **ยืนยัน** |
| **`createGeoPoint` ไม่ validate type lat/lng ก่อนเขียน** | **✅ ถูกต้อง** | **String อาจถูกเขียนลง Sheet ผิดรูปแบบ** |
| **Grid 3×3 ไม่พอถ้า radius \> 1.5 กม.** | **🟡 Should Fix** | **ปัจจุบัน radius=50m ยังไม่เป็นปัญหา ควรเพิ่ม comment** |

---

### **🤖 GLMz**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`updateGeoStats` โหลดทั้งชีต \+ ใช้ `headers.indexOf` แทน `GEO_IDX`** | **✅ ถูกต้อง** | **ขัด RULE 6 และ RULE 3 เหมือน Module 06/07** |
| **ส่วนอื่นถูกต้อง** | **✅ ถูกต้อง** | **ยืนยัน** |

---

### **🤖 Grok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`haversineDistanceM` ไม่มีในไฟล์นี้** | **❌ เข้าใจผิด** | **GAS shared scope — อยู่ใน `14_Utils.gs`** |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **Grid Key calculation ใน `findGeoCandidates_` ผิด Logic** | **✅ ถูกต้อง** | **Bug จริง — `Math.floor((lat + dlat * gridSize) / gridSize)` ≠ `Math.floor(lat/gridSize) + dlat` เมื่อเจอ Floating Point** |
| **`AI_CONFIG`, `APP_CONST` ไม่มีในไฟล์นี้** | **❌ เข้าใจผิด** | **GAS shared scope — อยู่ใน `01_Config.gs`** |
| **`gridSize = 0.01` ใช้ 2 ที่ควรเป็น constant** | **✅ ถูกต้อง** | **ควรประกาศ `const GEO_GRID_SIZE = 0.01`** |
| **`radius` fallback ควรมี default** | **🟡 Should Fix** | **ปัจจุบันใช้ \`** |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`resolveGeo` check \`\!lat** |  | **\!lng\` อาจตัดค่า edge case ทิ้ง** |
| **Logic และ Syntax ถูกต้องทั้งหมด** | **✅ ถูกต้องบางส่วน** | **Syntax ถูก แต่มี Bug MERGED filter, Grid Floating Point** |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **Template Literal ขาด backtick ทุกจุด** | **❌ ผิดอย่างสิ้นเชิง** | **โค้ดเราใช้ backtick ถูกต้องทุกจุด — AIStudio อ่านไฟล์ผิดพลาด** |
| **ตัวแปรไม่ได้ประกาศ** | **❌ ผิดอย่างสิ้นเชิง** | **GAS shared scope ทั้งหมด — AIStudio ไม่เข้าใจ GAS architecture** |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 08**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| **1** | **`findGeoCandidates_()`** | **Grid Key calculation: `Math.floor((lat + dlat * gridSize) / gridSize)` → `Math.floor(lat/gridSize) + dlat` (Floating Point Bug)** | **🔴 Critical** | **❌ ยังไม่ได้ทำ** |
| **2** | **`loadAllGeos_()`** | **เพิ่ม filter `&& r[GEO_IDX.STATUS] !== APP_CONST.STATUS_MERGED`** | **🔴 Critical** | **❌ ยังไม่ได้ทำ** |
| **3** | **`resolveGeo()`** | **lat/lng check เพิ่ม `typeof === 'number'` \+ `isNaN()` guard** | **🔴 Critical** | **❌ ยังไม่ได้ทำ** |
| **4** | **`resolveGeo()`** | **Confidence formula เพิ่ม `Math.max(0, Math.min(100, confidence))`** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **5** | **`updateGeoStats()`** | **โหลดเฉพาะ geo\_id column \+ ใช้ `GEO_IDX` แทน `headers.indexOf` ตอน write \+ เพิ่ม guard** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **6** | **`createGeoPoint()`** | **Validate `lat`/`lng` เป็น Number ก่อน appendRow** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **7** | **Global** | **ประกาศ `const GEO_GRID_SIZE = 0.01` แทน hardcode `0.01` ทั้ง 2 ที่** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **8** | **JSDoc `resolveGeo()`** | **เพิ่ม `OUT_OF_BOUNDS` ใน comment status list** | **🟢 Minor** | **❌ ยังไม่ได้ทำ** |
| **9** | **`createGeoPoint()`** | **Confidence default 85 — แยกตาม source: driver=80, maps=90, manual=75** | **🟢 Nice-to-have** | **❌ ยังไม่ได้ทำ** |
| **10** | **`findGeoCandidates_()`** | **เพิ่ม Comment เตือนว่า Grid 3×3 รองรับ radius สูงสุด \~1.5 กม.** | **🟢 Minor** | **❌ ยังไม่ได้ทำ** |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| **`06_PersonService.gs`** | **`updatePersonStats()`** | **Pattern เดียวกัน — โหลดทั้งชีต \+ `headers.indexOf` ขัด RULE 3/6 ต้องแก้ให้ consistent** | **❌ ต้องแก้** |
| **`07_PlaceService.gs`** | **`updatePlaceStats()`** | **Pattern เดียวกัน** | **❌ ต้องแก้** |
| **`14_Utils.gs`** | **`haversineDistanceM()`** | **หลาย Reviewer เข้าใจผิดว่าไม่มี — ยืนยันอยู่ใน 14 ใช้ได้ผ่าน GAS shared scope** | **✅ ถูกต้องแล้ว** |

---

## **📋 Module 09\_DestinationService.gs — ผล Review จาก 7 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`DEST_IDX.STATUS` อาจไม่ตรง `record_status`** | **❌ เข้าใจผิด** | **`DEST_IDX.STATUS = 10` ชี้ไปที่ `record_status` ใน `01_Config.gs` v002 ถูกต้องแล้ว** |
| **Partial Match (Person+Geo) เสี่ยง match มั่วถ้าคนเดียวไปหลายที่ใน Geo เดียวกัน** | **✅ ถูกต้อง** | **Business Logic Bug — ควรมี priority ให้ Person+Place+Geo ก่อน** |
| **`loadAllDestinations_` ไม่ filter STATUS** | **✅ ถูกต้อง** | **Bug จริง — โหลด ARCHIVED/MERGED เข้ามาด้วย** |
| **filter ใช้ `!== ARCHIVED` แทน `=== ACTIVE`** | **✅ ถูกต้อง** | **Status ว่าง `''` จะผ่าน filter ได้** |
| **\`deliveryDate** |  | **now`เสี่ยง`new Date(0)\`** |
| **`logDebug('DestService', ...)` ไม่ consistent** | **🟢 Minor** | **ยังไม่ได้ทำ** |
| **`isNew` ไม่เคย return `true`** | **✅ ถูกต้อง** | **Field เตรียมไว้แต่ไม่ได้ใช้ — สับสน** |
| **Trinity strict `===` อาจชนกับ null/''** | **🟡 Should Fix** | **ควร normalize ก่อนเปรียบเทียบ** |
| **\`lat** |  | **0\` เสี่ยงถ้า lat=0 จริง** |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`d.personId === personId` ควรใช้ `String()` คลุม** | **🟡 Should Fix** | **เพื่อกัน undefined/null** |
| **`new Date()` เรียก 2 ครั้ง ควรประกาศ `const now`** | **✅ ถูกต้อง** | **ควรแก้ใน `updateDestinationStats`** |
| **`getDominantDestByGeo` check `filtered.length > 0` ดีแล้ว** | **✅ ถูกต้อง** | **ยืนยัน** |
| **ไม่มี Merge/Archive function สำหรับ Destination** | **🟢 Nice-to-have** | **ยังไม่ได้ทำ** |

---

### **🤖 Qwen**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`loadAllDestinations_` ไม่ filter ARCHIVED/MERGED** | **✅ ถูกต้อง** | **Bug จริง — ต้องแก้** |
| **`resolveDestination` return FOUND แม้ ARCHIVED/MERGED** | **✅ ถูกต้อง** | **เป็นผลพวงจากไม่ filter ใน loader** |
| **`updateDestinationStats` โหลดทั้งชีต** | **✅ ถูกต้อง** | **ขัด RULE 6 เหมือน Module 06/07/08** |
| **`lat`/`lng` ไม่ parse Number ก่อนเขียน** | **✅ ถูกต้อง** | **String อาจถูกเขียนลง Sheet** |
| **`deliveryDate` อาจรับ Invalid Date** | **🟡 Should Fix** | **ควร validate ก่อน** |

---

### **🤖 GLMz**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`updateDestinationStats` โหลดทั้งชีต \+ `headers.indexOf` ขัด RULE 3/6** | **✅ ถูกต้อง** | **Pattern เดียวกับ Module 06/07/08 ต้องแก้** |
| **`loadAllDestinations_` ไม่ filter STATUS** | **✅ ถูกต้อง** | **Bug จริง** |
| **โบนัส: ถ้าแก้ loader แล้ว query functions ลบ filter ซ้ำได้** | **✅ ถูกต้อง** | **Clean code ดีกว่า** |

---

### **🤖 Grok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **ไม่มีข้อผิดพลาด** | **✅ ถูกต้องบางส่วน** | **Syntax ถูก แต่มี Logic Bug ที่ควรแก้** |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`resolveDestination` guard `!personId && !placeId && !geoId` ควรเป็น `||`** | **✅ ถูกต้อง** | **Bug จริง — `&&` ต้องว่างทั้ง 3 ถึงจะ reject ถ้าขาดตัวเดียวควร reject** |
| **`isNew` ไม่เคย return true** | **✅ ถูกต้อง** | **Field ไม่ได้ใช้จริง** |
| **`APP_CONST`, `AI_CONFIG` ไม่มีในไฟล์นี้** | **❌ เข้าใจผิด** | **GAS shared scope — อยู่ใน `01_Config.gs`** |
| **`loadAllDestinations_` ควร filter ก่อน map** | **✅ ถูกต้อง** | **Performance ดีกว่า** |
| **`route_label` ไม่ได้ map ใน `loadAllDestinations_`** | **✅ ถูกต้อง** | **Missing field ใน object** |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`resolveDestination` guard `&&` ควรเป็น `||` สำหรับ Trinity** | **✅ ถูกต้อง** | **Bug จริง — Trinity ต้องครบ 3 ถ้าขาดตัวใดควร reject** |
| **Logic และ Syntax ถูกต้องทั้งหมด** | **✅ ถูกต้องบางส่วน** | **มี Bug `loadAllDestinations_` filter และ `&&` guard** |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **ไม่พบข้อผิดพลาดใดเลย** | **✅ ถูกต้องบางส่วน** | **Syntax ถูก แต่มี Logic Bug ที่ควรแก้** |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 09**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| **1** | **`resolveDestination()`** | **Guard `!personId && !placeId && !geoId` → `!personId || !placeId || !geoId` — Trinity ต้องครบ 3** | **🔴 Critical** | **❌ ยังไม่ได้ทำ** |
| **2** | **`loadAllDestinations_()`** | **เพิ่ม filter `&& r[DEST_IDX.STATUS] !== APP_CONST.STATUS_ARCHIVED && r[DEST_IDX.STATUS] !== APP_CONST.STATUS_MERGED`** | **🔴 Critical** | **❌ ยังไม่ได้ทำ** |
| **3** | **`updateDestinationStats()`** | **โหลดเฉพาะ dest\_id column \+ ใช้ `DEST_IDX` แทน `headers.indexOf` \+ เพิ่ม guard \+ `const now = new Date()` ครั้งเดียว** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **4** | **`getDestsByPersonId/PlaceId/PersonAndPlace()`** | **เปลี่ยน `d.status !== STATUS_ARCHIVED` → `d.status === STATUS_ACTIVE`** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **5** | **`loadAllDestinations_()`** | **เพิ่ม `route_label` ใน map object — ปัจจุบัน missing field** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **6** | **`createDestination()`** | **`deliveryDate || now` → `deliveryDate instanceof Date ? deliveryDate : now`** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **7** | **`createDestination()`** | **`lat || 0` → `Number(lat) || 0` และ Validate ก่อน appendRow** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **8** | **`resolveDestination()`** | **Trinity `===` normalize null/'' ก่อนเปรียบเทียบ `(personId || '')`** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **9** | **`resolveDestination()`** | **`isNew` field — ใช้จริงหรือลบออก ปัจจุบัน return `false` ทุกกรณีทำให้ สับสน** | **🟢 Minor** | **❌ ยังไม่ได้ทำ** |
| **10** | **`logDebug` ทุกจุด** | **`'DestService'` → `'DestinationService'` ให้ consistent กับชื่อไฟล์** | **🟢 Minor** | **❌ ยังไม่ได้ทำ** |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| **`06_PersonService.gs`** | **`updatePersonStats()`** | **Pattern เดียวกัน — ขัด RULE 3/6 ต้องแก้ทุกไฟล์ให้ consistent** | **❌ ต้องแก้** |
| **`07_PlaceService.gs`** | **`updatePlaceStats()`** | **Pattern เดียวกัน** | **❌ ต้องแก้** |
| **`08_GeoService.gs`** | **`updateGeoStats()`** | **Pattern เดียวกัน** | **❌ ต้องแก้** |

---

**พ📋 Module 10\_MatchEngine.gs — ผล Review จาก 6 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`resolvePlace(srcObj.rawAddress, srcObj.rawAddress)` — ส่ง parameter เดิม 2 ครั้ง** | **✅ ถูกต้อง** | **Bug จริง — แต่ `srcObj` ไม่มี field `rawPlaceName` ต้องพิจารณาเพิ่ม field ใน Module 04** |
| **Rule 1: `!hasGeo && geoResult.status !== 'NOT_FOUND'` — Logic ผิด** | **✅ ถูกต้อง** | **Bug จริง — `NOT_FOUND` จะไม่เข้า Rule 1 ควรแก้เป็น `!hasGeo`** |
| **Rule 5: Weight รวม 0.7+0.3+0.2 \= 1.2 เกิน 100%** | **✅ ถูกต้อง** | **Bug จริง — ควร normalize ให้รวมเป็น 1.0** |
| **Rule 7: ใช้ `hasPerson` แทน `isPersonOk`** | **✅ ถูกต้อง** | **Bug จริง — `hasPerson` รวม NEEDS\_REVIEW ด้วย ไม่ใช่ FOUND จริงๆ** |
| **`CREATE_NEW` เงื่อนไข `geoId || personId` หลวมเกิน** | **✅ ถูกต้อง** | **ควรมีอย่างน้อย geoId เพื่อสร้าง Destination** |
| **`placeResult.normResult.province` ไม่มีอยู่จริง** | **✅ ถูกต้อง** | **Bug จริง — `normalizePlaceName` return `{cleanPlace, placeType, notes}` ไม่มี province** |
| **`detectSameGeoMultiPerson` ใช้ `d.status`** | **❌ เข้าใจผิด** | **`loadAllDestinations_` map status ถูกต้องแล้ว** |
| **Date comparison timezone เสี่ยง** | **✅ ถูกต้อง** | **ควรใช้ `Utilities.formatDate()`** |
| **`executeDecision` ไม่มี guard null ก่อน update** | **🟡 Should Fix** | **ยังไม่ได้ทำ** |
| **`saveCheckpoint_` ไม่มี load กลับมา** | **✅ ถูกต้อง** | **Incomplete feature** |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`placeResult.normResult.province` เป็น Dead Code** | **✅ ถูกต้อง** | **Bug จริง — ยืนยันซ้ำกับ ChatGPT** |
| **`getGeoProvince_` เรียกซ้ำใน loop ทุก Rule 3** | **✅ ถูกต้อง** | **ควรเรียกครั้งเดียวนอก loop** |
| **`getSameDayDestinations` ไม่มี guard กรณี `deliveryDate` invalid** | **✅ ถูกต้อง** | **ควรเพิ่ม Date validation** |
| **`processOneRow` ถูกเรียกซ้ำ 2 ครั้งใน Pipeline** | **❌ เข้าใจผิด** | **`getUnprocessedRows()` กรอง Invoice ที่มีใน FACT แล้วออก — ไม่ซ้ำจริง** |
| **`saveCheckpoint_` ไม่มี resume logic** | **✅ ถูกต้อง** | **Incomplete feature** |
| **`detectSameGeoMultiPerson` ไม่ได้ใช้ใน Engine** | **✅ สังเกตถูก** | **สร้างไว้แต่ยังไม่ได้เรียกจาก makeMatchDecision** |

---

### **🤖 GLMz**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`resolvePlace(srcObj.rawAddress, srcObj.rawAddress)` ผิด** | **✅ ถูกต้อง** | **Bug จริง — แนะนำแก้เป็น `resolvePlace(srcObj.rawAddress, srcObj.province)`** |
| **`getSameDayDestinations` โหลดทั้งชีต 31 คอลัมน์ ขัด RULE 6** | **✅ ถูกต้อง** | **ควรอ่านเฉพาะคอลัมน์ที่จำเป็น** |

---

### **🤖 Grok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`enqueueReview` ไม่ได้ define ในไฟล์นี้** | **❌ เข้าใจผิด** | **GAS shared scope — อยู่ใน `12_ReviewService.gs`** |
| **`upsertFactDelivery` ไม่ได้ define ในไฟล์นี้** | **❌ เข้าใจผิด** | **GAS shared scope — อยู่ใน `11_TransactionService.gs`** |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`resolvePlace(srcObj.rawAddress, srcObj.rawAddress)` ผิด** | **✅ ถูกต้อง** | **Bug จริง** |
| **`resolvePerson/resolvePlace/resolveGeo` ฯลฯ ไม่มีในไฟล์นี้** | **❌ เข้าใจผิด** | **GAS shared scope** |
| **`srcObj.hasGeo` อาจไม่มีใน srcObj** | **❌ เข้าใจผิด** | **`buildSourceObj_` ใน Module 04 สร้าง `hasGeo` ไว้แล้ว** |
| **Rule 7/7b เงื่อนไขซ้อนกัน ควรรวม** | **🟡 Nice-to-have** | **Logic ถูกแต่อ่านยาก** |
| **`APP_CONST`, `FACT_IDX` ต้องมี fallback** | **❌ เข้าใจผิด** | **GAS shared scope — อยู่ใน `01_Config.gs`** |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **วิจารณ์ Module 09 ซ้ำ (copy-paste ผิดไฟล์)** | **❌ ผิดอย่างสิ้นเชิง** | **mCopilot ส่ง Review ของ Module 09 มาแทน Module 10 — ข้อมูลไม่ถูกต้องทั้งหมด** |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **ไม่พบ Syntax Error** | **✅ ถูกต้อง** | **Syntax ถูก** |
| **Logic ถูกต้องทั้งหมด** | **❌ เข้าใจผิด** | **มี Logic Bug หลายจุดที่ควรแก้** |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 10**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| **1** | **`processOneRow()`** | **`resolvePlace(srcObj.rawAddress, srcObj.rawAddress)` → `resolvePlace(srcObj.rawAddress, srcObj.province)`** | **🔴 Critical** | **❌ ยังไม่ได้ทำ** |
| **2** | **`makeMatchDecision()` Rule 1** | **`!hasGeo && geoResult.status !== 'NOT_FOUND'` → `!hasGeo`** | **🔴 Critical** | **❌ ยังไม่ได้ทำ** |
| **3** | **`makeMatchDecision()` Rule 3** | **`placeResult.normResult.province` ไม่มีจริง → ใช้ `srcObj.province` แทน** | **🔴 Critical** | **❌ ยังไม่ได้ทำ** |
| **4** | **`makeMatchDecision()` Rule 5** | **Weight 0.7+0.3+0.2=1.2 → ปรับเป็น 0.6+0.25+0.15=1.0** | **🔴 Critical** | **❌ ยังไม่ได้ทำ** |
| **5** | **`makeMatchDecision()` Rule 7** | **`!hasPerson && !hasPlace` → `!isPersonOk && !isPlaceOk`** | **🔴 Critical** | **❌ ยังไม่ได้ทำ** |
| **6** | **`executeDecision()` CREATE\_NEW** | **`if (geoId || personId)` → `if (geoId && (personId || placeId))`** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **7** | **`getSameDayDestinations()`** | **โหลดทั้งชีต 31 col → อ่านเฉพาะ DELIVERY\_DATE \+ GEO\_ID columns ขัด RULE 6** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **8** | **`getSameDayDestinations()`** | **Date compare เพิ่ม `Utilities.formatDate()` แทน `toDateString()` กัน timezone** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **9** | **`makeMatchDecision()` Rule 3** | **`getGeoProvince_` เรียกซ้ำทุก call → เรียกครั้งเดียวก่อนเข้า Rule** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **10** | **`runMatchEngine()`** | **เพิ่ม `loadCheckpoint_` เพื่อ resume จากจุดที่ save ไว้** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **11** | **`executeDecision()`** | **เพิ่ม null guard ก่อน `updateDestinationStats(destId,...)`** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **12** | **`makeMatchDecision()`** | **`detectSameGeoMultiPerson` สร้างไว้แต่ไม่ได้เรียกใช้ — integrate หรือ document** | **🟢 Minor** | **❌ ยังไม่ได้ทำ** |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| **`04_SourceRepository.gs`** | **`buildSourceObj_()`** | **`processOneRow` ต้องการ field แยกสำหรับ Place Name — `rawAddress` ปัจจุบันทำหน้าที่ทั้ง Address และ Place Name ควรพิจารณาเพิ่ม field `rawPlaceName` หรือ refactor** | **❌ ต้องพิจารณา** |
| **`05_NormalizeService.gs`** | **`normalizePlaceName()`** | **Rule 3 ต้องการ province จาก normResult — ปัจจุบัน return `{cleanPlace, placeType, notes}` ไม่มี province ควรเพิ่มหรือใช้ `srcObj.province` แทน** | **❌ ต้องพิจารณา** |

---

## **📋 Module 11\_TransactionService.gs — ผล Review จาก 5 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`resolvePlace(srcObj.rawAddress, srcObj.rawAddress)`** | **❌ ผิดไฟล์** | **จุดนี้อยู่ใน Module 10 ไม่ใช่ Module 11 — ChatGPT ส่ง Review ผิดไฟล์มาทั้งหมด** |
| **Rule 5 weight 120%** | **❌ ผิดไฟล์** | **เป็น Bug ใน Module 10 ไม่ใช่ Module 11** |
| **Rule 1 Logic** | **❌ ผิดไฟล์** | **เป็น Bug ใน Module 10 ไม่ใช่ Module 11** |
| **`srcObj.hasGeo` ไม่มี** | **❌ ผิดไฟล์** | **เป็น Bug ใน Module 10 ไม่ใช่ Module 11** |
| **`createPlace` province mismatch** | **❌ ผิดไฟล์** | **เป็น Bug ใน Module 10 ไม่ใช่ Module 11** |

**สรุป: ChatGPT ส่ง Feedback ของ Module 10 มาทั้งหมด — ข้อมูลใช้อ้างอิงสำหรับ Module 11 ไม่ได้**

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **Update row อาจ "ข้อมูลผิดตำแหน่ง" ถ้า Schema เปลี่ยน** | **❌ เข้าใจผิด** | **โค้ดใช้ `FACT_IDX.*` index ทั้งหมด ไม่ได้ใช้ position จาก schema โดยตรง — RULE 3 ถูกต้อง** |
| **`findFactRowByInvoice_` วน Loop ช้าถ้าข้อมูลหลักหมื่น** | **✅ ถูกต้อง** | **ควรทำ Cache Map Invoice → RowNumber** |
| **`getGeoLatLng_` เรียกสองครั้ง** | **✅ ถูกต้อง** | **Bug จริง — `getGeoLatLng_(geoId).lat` และ `getGeoLatLng_(geoId).lng` เรียก `loadAllGeos_` ซ้ำ** |
| **`FACT_IDX.PERSON_NAME`, `FACT_IDX.PLACE_NAME` missing** | **❌ เข้าใจผิด** | **Fields เหล่านี้ไม่มีใน Schema ของเรา ไม่ใช่ Missing แต่ไม่ได้ออกแบบไว้** |
| **`APP_CONST`, `FACT_IDX` ควรมี fallback `?.`** | **❌ เข้าใจผิด** | **GAS shared scope — มีใน `01_Config.gs` ใช้ได้ทันที** |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`new Array().fill('')` แล้ว assign บางตัว — columns ว่างตรงกลาง** | **❌ เข้าใจผิด** | **ตั้งใจแล้ว — `fill('')` initialize ทุก column เป็น empty string ก่อน แล้ว assign เฉพาะที่มีข้อมูล column ว่างคือไม่มีข้อมูลเป็นของตั้งใจ** |
| **`getGeoLatLng_` เรียกซ้ำ 2 ครั้ง** | **✅ ถูกต้อง** | **Bug จริง** |
| **`resolvedLat` fallback ปลอดภัย** | **✅ ถูกต้อง** | **ควรแก้ให้ชัดเจนกว่านี้** |
| **`APP_CONST`, `FACT_IDX`, `SCHEMA.FACT_DELIVERY` ไม่มีในไฟล์** | **❌ เข้าใจผิด** | **GAS shared scope** |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`getGeoLatLng_` คืน `{lat:0, lng:0}` เมื่อไม่เจอ → พิกัด 0,0 กลางมหาสมุทร** | **✅ ถูกต้อง** | **Bug จริง — ควร fallback ไปใช้ `srcObj.rawLat/rawLng` แทน** |
| **Syntax และ Logic ถูกต้องทั้งหมด** | **✅ ถูกต้อง** | **ยืนยัน** |
| **index mapping `FACT_IDX` ถูกต้อง** | **✅ ถูกต้อง** | **ยืนยัน** |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| **`MATCH_STATUS = decision.action` ควรเป็น `decision.status`** | **❌ เข้าใจผิด** | **`decision` object จาก `makeMatchDecision` มี field `{action, reason, confidence, priority}` — ไม่มี `status` field เลย การใช้ `decision.action` ถูกต้องแล้ว** |
| **`getGeoLatLng_` เรียกซ้ำ 2 ครั้ง** | **✅ ถูกต้อง** | **Bug จริง ควรเรียกครั้งเดียว** |
| **`String(invoiceNo).trim()` ใน loop ทุกรอบ** | **✅ ถูกต้อง** | **ควร extract ออกมาก่อน loop** |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 11**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| **1** | **`getGeoLatLng_()`** | **คืน `null` แทน `{lat:0, lng:0}` เมื่อไม่เจอ — ป้องกัน 0,0 กลางมหาสมุทรลง FACT** | **🔴 Critical** | **❌ ยังไม่ได้ทำ** |
| **2** | **`upsertFactDelivery()`** | **เรียก `getGeoLatLng_(geoId)` ครั้งเดียว แล้ว destructure `.lat` / `.lng` แยก แทนเรียก 2 ครั้ง** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **3** | **`upsertFactDelivery()`** | **Fallback: ถ้า `getGeoLatLng_` คืน null ให้ใช้ `srcObj.rawLat/rawLng` แทน** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **4** | **`findFactRowByInvoice_()`** | **`String(invoiceNo).trim()` → extract เป็น `targetInvoice` นอก loop** | **🟡 Should Fix** | **❌ ยังไม่ได้ทำ** |
| **5** | **`findFactRowByInvoice_()`** | **พิจารณา Cache Map Invoice→RowNumber ป้องกัน O(n) loop เมื่อ FACT\_DELIVERY ใหญ่** | **🟢 Nice-to-have** | **❌ ยังไม่ได้ทำ** |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

**ไม่มีผลกระทบต่อ Module 00–10 ที่วิเคราะห์ไปแล้วครับ**

---

## **📋 Module 12\_ReviewService.gs — ผล Review จาก 3 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `resolvePlace(rawPlaceName, rawPlaceName)` ส่ง param ซ้ำ | ✅ **ถูกต้อง** | Bug จริง — ควรส่ง address จาก `rowData[REVIEW_IDX.RAW_SYS_ADDR]` แทน |
| `headers.indexOf('status')` case-sensitive → \-1 → เขียน col 0 | ✅ **ถูกต้อง** | Bug จริง — ควรใช้ `REVIEW_IDX.STATUS + 1` แทน |
| `applyAllPendingDecisions` filter `=== 'In_Review'` แต่ enqueue ใส่ `'Pending'` | ✅ **ถูกต้อง** | Bug Critical — ฟังก์ชันนี้ไม่ทำงานเลย |
| `hasGeo` ไม่เช็ค lng | ✅ **ถูกต้อง** | Bug จริง — ควร `!!(Number(lat) && Number(lng))` |
| `ESCALATE` case ไม่ set status → ตกไป `setValue('Done')` | ✅ **ถูกต้อง** | Bug จริง — ควร `setValue('Escalated')` แล้ว `return` |
| `enqueueReview` RAW\_PLACE กับ RAW\_SYS\_ADDR ใส่ค่าเดียวกัน | ✅ **ถูกต้อง** | Bug จริง — `srcObj` ไม่มี field `rawPlaceName` แยก |
| `CAND_PERSONS` เก็บแค่ ID เดี่ยว | ✅ **ถูกต้อง** | ควรเป็น JSON array |
| `CREATE_NEW` province ว่าง | ✅ **ถูกต้อง** | Data จะ loose downstream |
| `generateQualityReport` `doneCount = totalFact - stats.pending` | ✅ **ถูกต้อง** | Approximation เกินจริง |
| `highlightHighPriorityReviews` ยิง `setBackground` ทีละแถว | 🟡 **Should Fix** | Performance issue |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `applyAllPendingDecisions` filter `In_Review` ไม่ตรง initial status `Pending` | ✅ **ถูกต้อง** | Bug Critical — ยืนยันซ้ำกับ ChatGPT |
| `resolvePlace(rawPlaceName, rawPlaceName)` ผิด | ✅ **ถูกต้อง** | Bug จริง |
| `CREATE_NEW` `rawAddress: rawPlaceName` สลับ field | ✅ **ถูกต้อง** | Bug จริง |
| `generateQualityReport` `doneCount` ไม่แม่นยำ | ✅ **ถูกต้อง** | Approximation |
| Logic ถูกต้องส่วนอื่น | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `CREATE_NEW` `srcObj` ขาด `invoiceNo` และ `sourceRow` | ✅ **ถูกต้อง** | Bug Critical — ทำให้ `upsertFactDelivery` สร้าง FACT record ไม่มี reference |
| `applyAllPendingDecisions` filter `In_Review` | ✅ **ถูกต้อง** | Bug Critical — แนะนำแก้เป็น `status !== 'Done'` |
| `headers.indexOf('status')` case-sensitive | ✅ **ถูกต้อง** | Bug จริง — ใช้ `REVIEW_IDX.STATUS + 1` |
| `const srcObj` และ `const personResult` ใน `switch case` ไม่มี block scope `{}` | ✅ **ถูกต้อง** | ES6 Syntax Hazard — ควรครอบด้วย `{}` |
| `resolvePlace(rawPlaceName, rawPlaceName)` ผิด | ✅ **ถูกต้อง** | Bug จริง |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 12**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `applyAllPendingDecisions()` | `status === 'In_Review'` → `status !== 'Done'` — กัน Skip ทุก Pending row | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 2 | `applyReviewDecision()` | `headers.indexOf('status')` → `REVIEW_IDX.STATUS + 1` และ `headers.indexOf('reviewed_at')` → `REVIEW_IDX.REVIEWED_AT + 1` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 3 | `applyReviewDecision()` CREATE\_NEW | `srcObj` ขาด `invoiceNo` และ `sourceRow` — ทำให้ FACT record ไม่มี reference | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 4 | `applyReviewDecision()` CREATE\_NEW | `rawAddress: rawPlaceName` → `rawAddress: String(rowData[REVIEW_IDX.RAW_SYS_ADDR] || '')` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 5 | `applyReviewDecision()` CREATE\_NEW | `resolvePlace(rawPlaceName, rawPlaceName)` → `resolvePlace(rawPlaceName, rawAddressFromSheet)` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 6 | `applyReviewDecision()` ESCALATE | เพิ่ม `setValue('Escalated')` แล้ว `return` แทนที่จะตกไป `setValue('Done')` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 7 | `applyReviewDecision()` CREATE\_NEW | ครอบ `case` ด้วย `{}` block scope กัน ES6 `const` ซ้อน scope | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 8 | `applyReviewDecision()` CREATE\_NEW | `hasGeo` เพิ่มเช็ค lng ด้วย: `!!(Number(lat) && Number(lng))` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 9 | `enqueueReview()` | `RAW_PLACE = srcObj.rawAddress` → ใช้ `srcObj.rawPlaceName || srcObj.rawAddress` เมื่อ Module 04 เพิ่ม field `rawPlaceName` | 🟡 Should Fix | ❌ รอ Module 04 |
| 10 | `enqueueReview()` | `CAND_PERSONS/PLACES/GEOS` → เก็บเป็น `JSON.stringify([id])` แทน ID เดี่ยว | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 11 | `generateQualityReport()` | `doneCount = totalFact - stats.pending` → ควรนับจาก FACT\_DELIVERY ตาม `MATCH_STATUS` แทน | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 12 | `highlightHighPriorityReviews()` | เปลี่ยนจาก setBackground ทีละแถว → Batch collect ranges แล้ว setBackground ทีเดียว | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| `04_SourceRepository.gs` | `buildSourceObj_()` | ต้องเพิ่ม field `rawPlaceName` แยกจาก `rawAddress` เพื่อให้ Module 10, 12 ใช้ได้ถูกต้อง | ❌ ต้องเพิ่ม |
| `10_MatchEngine.gs` | `processOneRow()` | Pattern เดียวกัน — `resolvePlace(srcObj.rawAddress, srcObj.rawAddress)` ต้องแก้ด้วย | ❌ ต้องแก้ |

---

## **📋 Module 13\_ReportService.gs — ผล Review จาก 6 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `resolvePlace` param ผิด, GeoService check, TransactionService เรียกซ้ำ ฯลฯ | ❌ **ผิดไฟล์** | ChatGPT ส่ง Review ข้ามไฟล์ทั้งหมด — ไม่มีจุดใดเป็นของ Module 13 |
| แนะนำ Central Validator / Retry Queue / Metrics | 🟢 **Nice-to-have** | เป็นแนวคิด System Level ไม่ใช่ Bug ใน Module 13 |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `totalFact` นับแถวทั้งหมดรวม Archived | ✅ **ถูกต้อง** | ควรกรองเฉพาะ `RECORD_STATUS = Active` |
| `getReviewStats()` load ชีตทุกครั้ง | 🟢 **Minor** | ยอมรับได้เพราะรันไม่บ่อย |
| `appendRow` สะสมไม่ clear เก่า | 🟡 **Should Fix** | เป็น design decision — ถ้าต้องการ History ก็ appendRow ถูก แต่ถ้าต้องการแค่ล่าสุดควร overwrite |
| `matchRate` นับ `CREATE_NEW` รวมด้วย อาจไม่ตรง intent | ✅ **ถูกต้อง** | ควรแยก `autoMatchRate` vs `processedRate` |
| `MATCH_STATUS` นอกเหนือ 4 ค่า ไม่มี default case | ✅ **ถูกต้อง** | ควรเพิ่ม `else unclassifiedCount++` |

---

### **🤖 GLMz**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SHEET.RPT_QUALITY` ควรเปลี่ยนเป็น `SHEET.RPT_DATA_QUALITY` | ❌ **เข้าใจผิด** | `SHEET.RPT_QUALITY` คือ **constant name** ใน `01_Config.gs` ซึ่งมีค่าเป็น `'RPT_DATA_QUALITY'` อยู่แล้ว — ไม่มีปัญหา |
| ส่วนอื่นถูกต้อง | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 Grok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `getReviewStats()` ไม่ได้ define ในไฟล์นี้ | ❌ **เข้าใจผิด** | GAS shared scope — อยู่ใน `12_ReviewService.gs` ใช้ได้ทันที |
| ส่วนอื่นถูกต้อง | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `matchRate` นับ `CREATE_NEW` รวม — ควรใช้แค่ `autoCount` | ✅ **ถูกต้อง** | หรือเปลี่ยนชื่อเป็น `processedRate` ให้ตรง intent |
| `appendRow` ไม่ clear เก่า | ✅ **ถูกต้อง** | เหมือน Perplexity |
| `FACT_IDX`, `SHEET.RPT_QUALITY` ควรมี fallback | ❌ **เข้าใจผิด** | GAS shared scope — มีใน `01_Config.gs` ทั้งหมด |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `reviewCount` จาก FACT \= "เคยส่ง Review" ไม่ใช่ "กำลังรอ" | ✅ **ถูกต้อง** | ควรดึงจาก `getReviewStats().pending` แทน |
| Syntax และ Logic ถูกต้องทั้งหมด | ✅ **ถูกต้อง** | ยืนยัน |
| `matchRate` คำนวณถูกต้องตาม intent | ❌ **เข้าใจผิด** | Intent ยังไม่ชัด — ขึ้นอยู่กับว่า `CREATE_NEW` ควรนับเป็น Match หรือไม่ |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `\\n` ใน `buildFullQualityReport` Alert | ❌ **เข้าใจผิด** | ตรวจโค้ดจริงแล้ว — Module 13 ใช้ Template Literal `` ` `` \+ `\n` ถูกต้องแล้ว ไม่มี `\\n` ผิด |
| `ui.alert()` พังถ้ารันผ่าน Trigger อัตโนมัติ | ✅ **ถูกต้อง** | ควรเพิ่ม guard ตรวจว่ามี UI context ก่อนเรียก alert |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 13**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `buildFullQualityReport()` | แยก `matchRate` → `autoMatchRate = autoCount/total` และ `processedRate = (autoCount+newCount)/total` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 2 | `buildFullQualityReport()` | `reviewCount` จาก FACT → เปลี่ยนใช้ `getReviewStats().pending` แทน เพื่อสะท้อน "กำลังรอ" จริงๆ | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 3 | `buildFullQualityReport()` | `totalFact` → กรอง `RECORD_STATUS = Active` แทนนับทุกแถว | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 4 | `buildFullQualityReport()` | `statusData.forEach` เพิ่ม `else { unclassifiedCount++ }` สำหรับ MATCH\_STATUS นอกเหนือ 4 ค่า | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 5 | `buildFullQualityReport()` | เพิ่ม guard ก่อน `ui.alert()` — ถ้ารันผ่าน Trigger จะ Error | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 6 | `buildFullQualityReport()` | `appendRow` สะสม — พิจารณาใช้ overwrite แทนถ้าต้องการแค่ล่าสุด | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |
| 7 | `countSheetRows_()` | พิจารณากรอง Active rows แทนนับทั้งหมด | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

ไม่มีผลกระทบต่อ Module 00–12 ที่วิเคราะห์ไปแล้วครับ

---

## **📋 Module 14\_Utils.gs — ผล Review จาก 7 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `diceCoefficient` ใช้ Set แทน Multiset — ตัดซ้ำทิ้ง | ✅ **สังเกตถูก แต่ตั้งใจแล้ว** | ชื่อไทยไม่ค่อยซ้ำ Bigram ธรรมดา — AIStudio ยืนยันว่าเป็น Design Choice ที่ถูกต้อง |
| `haversineDistanceM` `aVal > 1` → `sqrt(negative)` → NaN | ✅ **ถูกต้อง** | Edge Case จริง ควรเพิ่ม `Math.min(1, aVal)` |
| `isValidLatLng` \`&& → |  | \` |
| `toThaiDateStr` ไม่เช็ค Invalid Date | ✅ **ถูกต้อง** | คืน `NaN/NaN/NaN` ถ้า input ไม่ใช่วันที่ |
| `parseLatLng` เพิ่ม separator `|` และ space | 🟢 **Nice-to-have** | ยังไม่ได้ทำ |
| `levenshteinDistance` Memory O(n\*m) | 🟢 **Nice-to-have** | ชื่อคน/สถานที่ไม่เกิน 50 ตัว ยังไม่จำเป็น |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| Thai Unicode สระ/วรรณยุกต์อาจทำให้ Bigram ผิดพลาด | ✅ **สังเกตถูก** | ควร NFC Normalize ก่อนเข้า `buildBigramSet_` |
| `levenshteinDistance` Memory Limit | 🟢 **Minor** | ชื่อไม่ยาวเกิน 50 ตัว ยอมรับได้ |
| Syntax และ Logic ถูกต้องทั้งหมด | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 Qwen**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `diceCoefficient` ควรเพิ่ม guard หาร 0 | ✅ **สังเกตถูก แต่ไม่จำเป็น** | โค้ดปัจจุบัน `return 0` ก่อนถ้า `length < 2` อยู่แล้ว — ไม่มีทางหาร 0 |
| Syntax และ Math ถูกต้องทั้งหมด | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 GLMz / Grok / mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| ไม่พบข้อผิดพลาดใดเลย | ✅ **ถูกต้อง** | ยืนยัน — ไฟล์นี้สะอาดมาก |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `levenshteinDistance` — `matrix[0][j] = j` ผิด เพราะ `matrix[0] = [i]` มีแค่ 1 element | ❌ **เข้าใจผิด** | JavaScript Array สามารถ assign index ใดก็ได้เกิน length ปัจจุบัน — `matrix[0][j] = j` ทำงานถูกต้องสมบูรณ์ ไม่ใช่ Bug |
| `diceCoefficient` เขียน filter แทน forEach | 🟢 **Style only** | ทั้งสองแบบถูกต้องและเร็วพอกัน |
| `generateMd5Hash` ใช้ `base64Encode` แทน | ❌ **ไม่ถูกต้อง** | `base64Encode` คืน Base64 ไม่ใช่ Hex — ระบบเราต้องการ Hex สำหรับ Cache Key |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `toThaiDateStr` ไม่เช็ค Invalid Date → คืน `NaN/NaN/NaN` | ✅ **ถูกต้อง** | ควรเพิ่ม `if (isNaN(d.getTime())) return ''` |
| `haversineDistanceM` Floating Point `aVal > 1` | ✅ **ถูกต้อง** | ควรเพิ่ม `Math.min(1, aVal)` |
| `diceCoefficient` ใช้ Set ถูกต้องสำหรับชื่อไทย | ✅ **ถูกต้อง** | ยืนยัน — Design Choice ที่ถูกต้อง |
| Syntax ถูกต้องทั้งหมด | ✅ **ถูกต้อง** | ยืนยัน |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 14**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `isValidLatLng()` | `numLat === 0 && numLng === 0` → `numLat === 0 || numLng === 0` — ป้องกัน lat=0.1, lng=0 ผ่าน | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 2 | `haversineDistanceM()` | เพิ่ม `const safeAVal = Math.min(1, Math.max(0, aVal))` ก่อน `Math.sqrt()` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 3 | `toThaiDateStr()` | เพิ่ม `if (isNaN(d.getTime())) return ''` หลัง `new Date(date)` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 4 | `buildBigramSet_()` | พิจารณาเพิ่ม Unicode NFC Normalize ก่อน Bigram สำหรับชื่อไทยที่มีสระซ้อน | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |
| 5 | `parseLatLng()` | เพิ่ม separator `|` และ `\s` ใน regex รองรับรูปแบบอื่น | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| `08_GeoService.gs` | `resolveGeo()` | ใช้ `isValidLatLng()` — หลัง fix \`&&→ |  |

---

## **📋 Module 15\_GoogleMapsAPI.gs — ผล Review จาก 5 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `clearMapsCache()` — `removeAll(['GEO_', 'RGEO_'])` ใช้ Prefix ไม่ได้ | ✅ **ถูกต้อง** | Bug จริง — `removeAll()` ต้องการ Exact Key เท่านั้น |
| `reverseGeocode()` vs `getFromSheetCache_` — Schema ไม่ตรงกัน | ✅ **ถูกต้อง** | Bug จริง — `province/district` หายเมื่อ Cache Hit |
| `geocodeAddress()` — `break` แม้ API ล้มเหลว ทำให้ไม่ Retry | ✅ **ถูกต้อง** | Bug จริง — ควรย้าย `break` เข้าไปใน `if (status === 'OK')` |
| `reverseGeocode()` ไม่มี `.setRegion('TH')` | ✅ **ถูกต้อง** | ควรเพิ่ม |
| `getRouteDistanceKm()` `legs` อาจเป็น undefined | ✅ **ถูกต้อง** | ควรเพิ่ม guard |
| Address ไม่ Normalize ก่อน Hash | 🟡 **Should Fix** | "บางนา กรุงเทพ" vs "บางนา, กรุงเทพ" \= คนละ Cache Key |
| `extractAddrComponent_` ใช้แค่ `long_name` | 🟢 **Nice-to-have** | เพิ่ม fallback \` |
| `getGeoLatLng_` เรียก 2 รอบ | ❌ **ผิดไฟล์** | Bug นี้อยู่ใน Module 11 ไม่ใช่ Module 15 |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `reverseGeocode` cache ไม่เก็บ `province/district` | ✅ **ถูกต้อง** | Bug จริง — ยืนยันซ้ำ |
| `clearMapsCache` — `removeAll` ไม่ support Wildcard | ✅ **ถูกต้อง** | Bug จริง |
| CacheService 100KB limit อาจเต็ม | ✅ **สังเกตถูก** | Sheet Cache เป็น Fallback ที่ดีอยู่แล้ว |
| `getRouteDistanceKm` โควต้าสูง | 🟡 **Should Warn** | ควรเพิ่ม Comment เตือน |
| Syntax ถูกต้องทั้งหมด | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 Qwen**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `clearMapsCache` — `removeAll` prefix ไม่ทำงาน | ✅ **ถูกต้อง** | Bug จริง |
| `reverseGeocode` cache schema mismatch | ✅ **ถูกต้อง** | Bug จริง |
| `getRouteDistanceKm` คำนวณ km — `Math.round(totalMeters / 100) / 10` | ✅ **ถูกต้อง** | คำนวณถูกแต่อ่านยาก ควรเขียนเป็น `Math.round(totalMeters / 1000 * 10) / 10` |
| `getFromSheetCache_` Hardcode `latCol=2, lngCol=3, addrCol=4, hitCol=7` | ✅ **ถูกต้อง** | ขัด RULE 3 ควรใช้ SCHEMA index แทน |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| สมบูรณ์ 100% ไม่มีจุดผิด | ❌ **เข้าใจผิด** | มี 2 Bug Critical ที่ Reviewer อื่นพบ — `removeAll` prefix ไม่ทำงาน และ `reverseGeocode` cache schema mismatch |
| `clearMapsCache` — `removeAll` ถูกต้อง | ❌ **เข้าใจผิด** | ไม่ถูกต้อง — `removeAll` ไม่ support prefix |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `reverseGeocode` vs cache schema mismatch — `lat/lng = 0`, `province/district` หาย | ✅ **ถูกต้อง** | Bug Critical — อธิบาย impact ชัดเจนที่สุด |
| `clearMapsCache` — `removeAll` prefix ไม่ทำงาน | ✅ **ถูกต้อง** | แนะนำให้ลบบรรทัดนั้นทิ้งได้เลย |
| Syntax ถูกต้องทั้งหมด | ✅ **ถูกต้อง** | ยืนยัน |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 15**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `reverseGeocode()` \+ `saveToSheetCache_()` \+ `getFromSheetCache_()` | Cache Schema ไม่รองรับ `province/district` — ต้องขยาย MAPS\_CACHE Schema เพิ่ม 2 column และปรับ save/get ให้ครบ | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 2 | `clearMapsCache()` | `removeAll(['GEO_', 'RGEO_'])` → ลบบรรทัดนี้ทิ้ง หรือเปลี่ยนเป็น `removeAll([exactKey1, exactKey2, ...])` — ปัจจุบัน RAM Cache ไม่ถูกลบเลย | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 3 | `geocodeAddress()` | `break` ย้ายเข้าใน `if (status === 'OK')` — ปัจจุบัน break แม้ API ล้มเหลวทำให้ไม่ Retry | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 4 | `reverseGeocode()` | เพิ่ม `.setRegion('TH')` ป้องกัน address format ต่างประเทศ | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 5 | `getRouteDistanceKm()` | เพิ่ม guard `if (!directions.routes[0].legs || !directions.routes[0].legs.length)` ก่อนใช้ | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 6 | `getFromSheetCache_()` | เปลี่ยน Hardcode `latCol=2, lngCol=3, addrCol=4, hitCol=7` → ใช้ `SCHEMA.MAPS_CACHE.indexOf(...)` แทน ป้องกัน index drift | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 7 | `geocodeAddress()` | Normalize address ก่อน Hash: `.replace(/\s+/g,' ').toLowerCase().trim()` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 8 | `extractAddrComponent_()` | เพิ่ม fallback `comp.short_name` กรณี `long_name` ว่าง | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |
| 9 | `getRouteDistanceKm()` | เพิ่ม Comment เตือนว่าใช้ API Quota สูง ไม่ควรรันใน Loop | 🟢 Minor | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| `02_Schema.gs` | `SCHEMA.MAPS_CACHE` | ต้องเพิ่ม `province` \[8\] และ `district` \[9\] ใน MAPS\_CACHE Schema — ปัจจุบัน 8 คอลัมน์ต้องเพิ่มเป็น 10 คอลัมน์ | ❌ ต้องแก้ |
| `01_Config.gs` | MAPS\_CACHE Schema | ถ้าเพิ่ม column ใน SCHEMA ต้องแน่ใจว่า `validateSchemaConsistency()` ผ่าน | ⚠️ ต้องตรวจสอบ |

---

## **📋 Module 16\_GeoDictionaryBuilder.gs — ผล Review จาก 6 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `lookupProvinceFromAddress` regex `จ\.` ไม่ครอบคลุม `จ เชียงใหม่` / `จังหวัดเชียงใหม่` | ✅ **ถูกต้อง** | Bug จริง — ควรใช้ \`(?:จ.? |
| `addr.includes(province)` เสี่ยง false positive | ✅ **ถูกต้อง** | "ชัย" match "ชัยภูมิ" ได้ |
| Fallback `buildPostcodeMapFromSheet_` ไม่ re-cache | ✅ **ถูกต้อง** | Bug จริง — Query Sheet ทุกครั้งที่ Cache หมด |
| `lookupByPostcode` ไม่ padStart leading zero | ✅ **ถูกต้อง** | "01000" vs "1000" \= mismatch |
| `buildGeoDictionary` Cache 3 อย่างใน try เดียว | ✅ **ถูกต้อง** | Bug จริง — ยืนยันซ้ำกับ AIStudio |
| `invalidateGeoDictCache` ใช้ exact key ถูกต้อง | ✅ **ถูกต้อง** | ต่างจาก Module 15 ที่ใช้ prefix — อันนี้ถูก |
| Cache Versioning | 🟢 **Nice-to-have** | ยังไม่ได้ทำ |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| Cache 100KB Limit อาจเต็ม | ✅ **สังเกตถูก** | มี try-catch รองรับแล้ว แต่ยัง Bug เรื่อง 3 ใน try เดียว |
| `lookupProvinceFromAddress` 77 จังหวัดเร็วพอ | ✅ **ถูกต้อง** | ยืนยัน — O(n) กับ 77 items ไม่ใช่ปัญหา |
| `buildGeoDictionary` alert ใช้งานไม่ได้ใน Trigger | ✅ **ถูกต้อง** | ควรเพิ่ม guard เหมือน Module 13 |
| Syntax และ Logic ถูกต้อง | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 Qwen**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| Hardcode `5` ใน `getRange(..., 5)` | ✅ **ถูกต้อง** | ขัด RULE 3 — ควรใช้ `SCHEMA[SHEET.SYS_TH_GEO].length` |
| `lookupProvinceFromAddress` includes false positive | ✅ **ถูกต้อง** | Bug จริง |
| Fallback ไม่ re-cache | ✅ **ถูกต้อง** | Bug จริง |
| `buildGeoDictionary` 3 cache ใน try เดียว | ✅ **ถูกต้อง** | Bug จริง |
| `TH_GEO_DISTRICTS` cache แต่ไม่มี getter ใช้ — Dead Code | ✅ **ถูกต้อง** | ยืนยัน — สร้าง cache แต่ไม่มี `getCachedDistricts_()` |

---

### **🤖 Grok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| Hardcode `5` ใน `getRange` ควรใช้ `SCHEMA.SYS_TH_GEO.length` | ✅ **ถูกต้อง** | ขัด RULE 3 |
| `lookupProvinceFromAddress` includes false positive | ✅ **ถูกต้อง (แต่บอกว่าเล็กน้อย)** | มีผลจริงในบางเคส |

---

### **🤖 xGrok**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `buildPostcodeMapFromSheet_` ก็มี hardcode `5` อีกที่ | ✅ **ถูกต้อง** | ครบ 2 จุดที่ Grok พบแค่จุดเดียว |
| `TH_GEO_DISTRICTS` cache แต่ไม่มี getter — Dead Code | ✅ **ถูกต้อง** | ยืนยัน |
| `Math.max(...Object.values(TH_GEO_IDX)) + 1` แทน `Object.keys().length` | 🟡 **Nice-to-have** | ใช้ได้ทั้งคู่ แต่ max+1 แม่นยำกว่าถ้า IDX ไม่ได้ sequential |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| สมบูรณ์ 100% ไม่มีจุดผิด | ❌ **เข้าใจผิด** | มี Bug จริง 4 จุดที่ Reviewer อื่นพบครบ |
| `invalidateGeoDictCache` removeAll ถูกต้อง | ✅ **ถูกต้อง** | ยืนยัน — ใช้ Exact Key ต่างจาก Module 15 |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `buildGeoDictionary` — Cache 3 อย่างใน try เดียว → POSTCODE ล้มแล้ว PROVINCES/DISTRICTS ไม่ถูก cache | ✅ **ถูกต้อง** | Bug Critical — อธิบาย impact ชัดที่สุด |
| `TH_GEO_DISTRICTS` cache แต่ไม่มี getter | ✅ **ถูกต้อง** | Incomplete feature |
| Syntax ถูกต้อง | ✅ **ถูกต้อง** | ยืนยัน |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 16**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `buildGeoDictionary()` | แยก try-catch เป็น 3 บล็อกแยกกัน — ป้องกัน POSTCODE ล้มแล้ว PROVINCES/DISTRICTS ไม่ถูก Cache | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 2 | `lookupProvinceFromAddress()` | Regex `จ\.` → \`(?:จ.? | จังหวัด)\\s\*`ครอบคลุม`จ เชียงใหม่`และ`จังหวัดเชียงใหม่\` | 🔴 Critical |
| 3 | `lookupProvinceFromAddress()` | `addr.includes(province)` เพิ่ม `&& province.length >= 4` กัน false positive | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 4 | `getCachedPostcodeMap_()` / `getCachedProvinces_()` | Fallback ต้อง re-cache ผลลัพธ์ก่อน return | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 5 | `lookupByPostcode()` | เพิ่ม `.padStart(5, '0')` ก่อน replace non-digit | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 6 | `buildPostcodeMapFromSheet_()` \+ `buildProvincesFromSheet_()` | Hardcode `5` / `1` → ใช้ `SCHEMA[SHEET.SYS_TH_GEO].length` และ `TH_GEO_IDX.PROVINCE + 1` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 7 | `buildGeoDictionary()` | เพิ่ม guard ก่อน `ui.alert()` กัน Error เมื่อรันจาก Trigger | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 8 | `TH_GEO_DISTRICTS` | เพิ่ม `getCachedDistricts_()` และ `lookupDistrictsByProvince()` หรือลบ cache ทิ้งถ้าไม่ใช้ | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 9 | Cache Versioning | เพิ่ม prefix version เช่น `v1_TH_GEO_POSTCODE` | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

ไม่มีผลกระทบต่อ Module 00–15 ที่วิเคราะห์ไปแล้วครับ

---

## **📋 Module 17\_SearchService.gs — ผล Review จาก 5 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| Normalize แล้วแต่ส่ง `rawPerson`/`rawPlace` เข้า `resolvePerson`/`resolvePlace` ทิ้งงาน normalize | ✅ **ถูกต้อง** | Bug จริง — `cleanName`/`cleanPlace` ถูกสร้างแล้วไม่ได้ใช้ |
| `resolvePlace(rawPlace, rawPlace)` ส่ง param ซ้ำ | ✅ **ถูกต้อง** | Bug จริง — ควรเป็น `resolvePlace(cleanPlace, rawPlace)` |
| `setBackground` ทีละแถวใน loop | ✅ **ถูกต้อง** | Performance Bug — ควรใช้ `setBackgrounds()` Batch |
| `existingLL.includes(',')` check หลวม | ✅ **ถูกต้อง** | ควรใช้ `parseLatLng` \+ `isValidLatLng` แทน |
| `SCG_API_FALLBACK` `destId: ''` แทน `null` | ✅ **ถูกต้อง** | ควรใช้ `null` ชัดเจนกว่า |
| `NOT_FOUND` return `(0,0)` อาจทำให้ Marker ตกทะเล | ✅ **ถูกต้อง** | ควรใช้ `lat: null, lng: null` |
| Confidence fix `92` ไม่สัมพันธ์กับ data | 🟢 **Nice-to-have** | ยังไม่ได้ทำ |
| Fuzzy fallback ยังไม่ใช้ Levenshtein/Dice ใน SearchService | 🟢 **Nice-to-have** | ยังไม่ได้ทำ |

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `getDestsByPersonAndPlace` กรอง ARCHIVED แล้ว ดีมาก | ✅ **ถูกต้อง** | ยืนยัน |
| `setBackground` ใน Loop ช้า — ควรใช้ `setBackgrounds()` | ✅ **ถูกต้อง** | Performance Bug |
| `runLookupEnrichment` ไม่มี Time Guard | ✅ **ถูกต้อง** | ควรเพิ่มเหมือน `runMatchEngine` |
| Batch Write `setValues` ดีมากแล้ว | ✅ **ถูกต้อง** | ยืนยัน |
| `existingLL.includes(',')` skip logic ดี (Idempotency) | ✅ **ถูกต้อง แต่ยังหลวม** | ChatGPT ชี้ว่า `"xxx,yyy"` ก็ผ่านได้ |

---

### **🤖 Qwen**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `resolvePlace(rawPlace, rawPlace)` ส่ง param ซ้ำ | ✅ **ถูกต้อง** | Bug จริง |
| `resolvePerson(rawPerson)` ควรส่ง `cleanName` แทน | ✅ **ถูกต้อง** | Bug จริง — normalize แล้วทิ้ง |
| `setBackground` loop ช้า | ✅ **ถูกต้อง** | Performance Bug |
| `DATA_IDX.LATLNG_ACTUAL + 1` ใช้ดีแล้ว | ✅ **ถูกต้อง** | ยืนยัน |
| `buildSearchResult_` ควรมี \` |  | 0\` สำหรับ lat/lng |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `resolvePlace(rawPlace, rawPlace)` → ควรเป็น `resolvePlace(cleanPlace, rawPlace)` | ✅ **ถูกต้อง** | Bug จริง — อธิบาย impact ชัดสุด |
| `dests[0]` assumption ว่าเรียง usageCount แล้ว | ✅ **ถูกต้อง** | `getDestsByPersonAndPlace()` sort อยู่แล้ว — แต่ควร explicit sort เพื่อความปลอดภัย |
| Syntax / Index ถูกต้องทั้งหมด | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `personResult?.personId` ควรใช้ Optional Chaining | ❌ **เข้าใจผิด** | `resolvePerson()` return Object เสมอ ไม่ return `null` — โค้ดในไฟล์ 06 confirm แล้ว ไม่ใช่ Bug |
| `resolvePlace(rawPlace, rawPlace)` param ซ้ำ | ✅ **ถูกต้อง** | Bug จริง |
| `setBackground` loop ทีละแถว \= Performance Killer | ✅ **ถูกต้อง** | Bug จริง — อธิบาย solution ชัดเจนที่สุด |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 17**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `findBestGeoByPersonPlace()` | `resolvePerson(rawPerson)` → `resolvePerson(rawPerson)` ยังโอเค แต่ `resolvePlace(rawPlace, rawPlace)` → `resolvePlace(cleanPlace, rawPlace)` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 2 | `findBestGeoByPersonPlace()` | `cleanName`/`cleanPlace` สร้างแล้วไม่ได้ส่งเข้า resolve — ต้องใช้จริงหรือลบออก | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 3 | `runLookupEnrichment()` | `setBackground` ทีละแถวใน loop → `setBackgrounds(bgMatrix)` Batch ทีเดียว | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 4 | `buildSearchResult_()` | `NOT_FOUND` return `0, 0` → `null, null` ป้องกัน Marker ตกทะเล | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 5 | `buildSearchResult_()` | `SCG_API_FALLBACK` `destId: ''` → `destId: null` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 6 | `runLookupEnrichment()` | `existingLL.includes(',')` → `parseLatLng(existingLL)` \+ `isValidLatLng()` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 7 | `runLookupEnrichment()` | เพิ่ม Time Guard ป้องกัน Timeout เมื่อ DAILY\_JOB มีหลาย 1,000 แถว | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 8 | `findBestGeoByPersonPlace()` Tier A | เพิ่ม explicit `.sort()` ก่อน `dests[0]` ป้องกัน assumption ผิด | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 9 | `buildSearchResult_()` | Confidence dynamic: `Math.min(95, 80 + Math.log10(usageCount + 1) * 10)` แทน hardcode | 🟢 Nice-to-have | ❌ ยังไม่ได้ทำ |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| `18_ServiceSCG.gs` | `applyMasterCoordinatesToDailyJob()` | เรียก `runLookupEnrichment()` ตรง — ต้องแน่ใจว่า Caller รองรับ `null` lat/lng จาก `buildSearchResult_` หลัง fix | ⚠️ ต้องตรวจสอบ |

---

## **📋 Module 18\_ServiceSCG.gs — ผล Review จาก 6 AI**

---

## **2️⃣ ตารางสรุป ถูก/ผิด แยกตาม Reviewer**

### **🤖 ChatGPT**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `removeAll(['GEO_', 'RGEO_'])` ใช้ผิดที่ | ❌ **ผิดไฟล์** | Bug นี้อยู่ใน Module 15 (`clearMapsCache`) ไม่ใช่ Module 18 |
| `headers.indexOf('status')` case-sensitive | ❌ **ผิดไฟล์** | อยู่ใน Module 12 (`applyReviewDecision`) ไม่ใช่ Module 18 |
| `getFromSheetCache_` return ไม่ตรง | ❌ **ผิดไฟล์** | อยู่ใน Module 15 ไม่ใช่ Module 18 |
| `resolvePlace(rawPlace, rawPlace)` ซ้ำ | ❌ **ผิดไฟล์** | อยู่ใน Module 17 ไม่ใช่ Module 18 |
| `setBackground` loop ช้า | ❌ **ผิดไฟล์** | อยู่ใน Module 17 ไม่ใช่ Module 18 |
| `haversineDistanceM` ไม่มี guard | ❌ **ผิดไฟล์** | อยู่ใน Module 14 ไม่ใช่ Module 18 |
| `fetchDataFromSCGJWD` — `shipmentString` Dead Variable | ✅ **ถูกต้อง** | Bug จริง — อ่านค่ามาแต่ไม่ได้ใช้ |

**สรุป:** ChatGPT ส่ง Review ข้ามไฟล์มาทั้งหมด — ถูกต้องจริงเพียง 1 จุด

---

### **🤖 Perplexity**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `fetchOneShipment_` Retry \+ Sleep ดีมาก | ✅ **ถูกต้อง** | ยืนยัน |
| Shipment เยอะอาจ Timeout | ✅ **ถูกต้อง** | ควรเพิ่ม Time Guard |
| `buildDailyJobRow_` Defensive mapping ดีมาก | ✅ **ถูกต้อง** | ยืนยัน |
| `clearAllSCGSheets_UI` confirm dialog ดีมาก | ✅ **ถูกต้อง** | ยืนยัน |
| `clearDailyJobLatLng` ล้างเฉพาะคอลัมน์ ดีมาก | ✅ **ถูกต้อง** | ยืนยัน |
| Syntax และ Logic ถูกต้อง | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 Qwen**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `DATA_IDX` ตรงกับ `SCHEMA.DAILY_JOB` | ✅ **ถูกต้อง** | ยืนยัน — ตรวจแล้วสอดคล้องกัน |
| `buildDailyJobRow_` mapping ครบ 29 คอลัมน์ | ✅ **ถูกต้อง** | ยืนยัน |
| `fetchDataFromSCGJWD` Timeout risk | ✅ **ถูกต้อง** | ควรเพิ่ม Time Guard |
| Syntax ถูกต้อง | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 mCopilot**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `resolvePlace(rawPlace, rawPlace)` ผิด | ❌ **ผิดไฟล์** | อยู่ใน Module 17 ไม่ใช่ Module 18 — mCopilot เข้าใจถูกแต่ระบุว่า "ไฟล์ 18 ไม่ต้องแก้ แต่ไฟล์ 17 ต้องแก้" ✅ ถูก |
| `SCHEMA.SHIPMENT_SUMMARY` vs `SHEET.SHIPMENT_SUM` naming inconsistency | ✅ **ถูกต้อง** | ชื่อไม่ตรงกัน แต่ไม่ใช่ Error เพราะ 02\_Schema v002 ใช้ key ชื่อชีตจริง `'สรุป_Shipment'` แล้ว |
| ทุกอย่างอื่นถูกต้อง | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 GenSpark**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `SCHEMA.SHIPMENT_SUMMARY` vs `SHEET.SHIPMENT_SUM` | ✅ **ถูกต้อง** | Naming inconsistency — แต่หลัง 02\_Schema v002 เปลี่ยน SCHEMA key เป็น `'สรุป_Shipment'` แล้ว `SCHEMA.SHIPMENT_SUMMARY` จะ \= undefined — ต้องแก้เป็น `SCHEMA[SHEET.SHIPMENT_SUM]` |
| `item.latLong || item.latlng` อาจไม่ครอบคลุม field names อื่น | ✅ **ถูกต้อง** | ควรเพิ่ม fallback |
| Syntax / Logic ถูกต้องทั้งหมด | ✅ **ถูกต้อง** | ยืนยัน |

---

### **🤖 AIStudio**

| ข้อที่วิจารณ์ | สถานะ | ข้อเท็จจริง |
| ----- | ----- | ----- |
| `fetchOneShipment_` — `muteHttpExceptions: true` \+ `break` ทำให้ไม่ Retry HTTP 5xx | ✅ **ถูกต้อง** | Bug Critical — เมื่อ API คืน 500/502/504, `UrlFetchApp` ไม่ throw แต่ `break` ออกจาก loop ทำให้ Retry ไม่เกิดขึ้นเลย |
| `shipmentString` Dead Variable | ✅ **ถูกต้อง** | อ่านค่ามาแต่ไม่ได้ใช้ |

---

## **3️⃣ รายการที่ต้องแก้ไขใน Module 18**

| \# | ฟังก์ชัน | สิ่งที่ต้องแก้ | ระดับ | สถานะ |
| ----- | ----- | ----- | ----- | ----- |
| 1 | `fetchOneShipment_()` | `muteHttpExceptions: true` \+ `break` ทำให้ไม่ Retry HTTP 5xx — ย้าย `httpCode` check เข้าใน `try` block | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 2 | `buildShipmentSummary()` | `SCHEMA.SHIPMENT_SUMMARY` → `SCHEMA[SHEET.SHIPMENT_SUM]` หรือ `getSheetHeaders(SHEET.SHIPMENT_SUM)` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 3 | `buildOwnerSummary()` | `SCHEMA.OWNER_SUMMARY` → `SCHEMA[SHEET.OWNER_SUMMARY]` หรือ `getSheetHeaders(SHEET.OWNER_SUMMARY)` | 🔴 Critical | ❌ ยังไม่ได้ทำ |
| 4 | `fetchDataFromSCGJWD()` | ลบ `const shipmentString = ...` Dead Variable | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 5 | `fetchDataFromSCGJWD()` | เพิ่ม Time Guard ป้องกัน Timeout เมื่อ Shipment เยอะ | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 6 | `buildDailyJobRow_()` | `item.latLong || item.latlng` เพิ่ม fallback `item.lat_long || item.latLongSCG` | 🟡 Should Fix | ❌ ยังไม่ได้ทำ |
| 7 | `clearDailyJobLatLng()` | ตรวจสอบให้แน่ใจ `DATA_IDX.LATLNG_ACTUAL + 1` ถูกต้อง (ยืนยันแล้ว ✅) | 🟢 Verify | ✅ ถูกต้อง |

---

## **🔗 กระทบ Module ที่วิเคราะห์ไปแล้ว**

| Module | ฟังก์ชัน | เหตุผล | สถานะ |
| ----- | ----- | ----- | ----- |
| `17_SearchService.gs` | `findBestGeoByPersonPlace()` | `applyMasterCoordinatesToDailyJob` เรียก `runLookupEnrichment()` จาก 17 — ถ้า `resolvePlace(rawPlace,rawPlace)` ยังผิดใน 17 จะทำให้ผล 18 พังด้วย | ❌ ต้องแก้ Module 17 ก่อน |
| `03_SetupSheets.gs` | `setupGroupTwoSheets_()` | Pattern เดียวกัน — `SCHEMA.SHIPMENT_SUMMARY` / `SCHEMA.OWNER_SUMMARY` ต้องเปลี่ยนเป็น `SCHEMA[SHEET.xxx]` | ❌ ต้องแก้ด้วย |

---

## **🏁 Review Mode ครบทุก Module แล้วครับ (00–18)**

